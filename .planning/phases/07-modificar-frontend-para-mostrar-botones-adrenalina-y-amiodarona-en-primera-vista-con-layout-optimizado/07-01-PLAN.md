---
phase: 07-modificar-frontend-para-mostrar-botones-adrenalina-y-amiodarona-en-primera-vista-con-layout-optimizado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AclsTracker/Controls/RhythmSelector.xaml
  - AclsTracker/Controls/MetronomePulse.xaml
  - AclsTracker/Controls/TimerCard.xaml
  - AclsTracker/Views/MainPage.xaml
autonomous: false
requirements:
  - UI-LAYOUT-01

must_haves:
  truths:
    - "ADRENALINA and AMIODARONA buttons are fully visible without scrolling on a standard Android screen (~360x800dp)"
    - "All 5 rhythm buttons (RCE, AESP, ASISTOLIA, TV, FV) render in a single horizontal row without wrapping"
    - "NUEVO CICLO and DEFIBRILAR appear side by side in equal-width columns"
    - "DataTrigger highlights on ADRENALINA (IsAdrenalinaSuggested) and AMIODARONA (IsAmiodaronaSuggested) still turn buttons red"
    - "BoolToOpacityConverter dims AMIODARONA when IsAmiodaronaEnabled is false"
    - "Metronome pulse circle animation still works correctly"
    - "ASISTOLIA text is not clipped or truncated"
  artifacts:
    - path: "AclsTracker/Controls/RhythmSelector.xaml"
      provides: "Single-row rhythm selector using Grid with * columns"
      contains: "ColumnDefinitions=\"*, *, *, *, *\""
    - path: "AclsTracker/Controls/MetronomePulse.xaml"
      provides: "Reduced circle size (45px grid, 40px ellipse)"
      contains: "HeightRequest=\"45\""
    - path: "AclsTracker/Controls/TimerCard.xaml"
      provides: "Compressed timer card (Padding=4, FontSize=20)"
      contains: "Padding=\"4\""
    - path: "AclsTracker/Views/MainPage.xaml"
      provides: "2x2 action button grid, reduced VSL spacing"
      contains: "NUEVO CICLO"
  key_links:
    - from: "AclsTracker/Views/MainPage.xaml"
      to: "MainPage.xaml.cs"
      via: "x:Name=DefibrilarButton"
      pattern: "x:Name=\"DefibrilarButton\""
    - from: "AclsTracker/Views/MainPage.xaml"
      to: "MainViewModel"
      via: "DataTrigger bindings for drug suggestion"
      pattern: "IsAdrenalinaSuggested|IsAmiodaronaSuggested"
---

<objective>
Compress the MainPage vertical layout so ADRENALINA and AMIODARONA buttons are visible without scrolling on standard Android screens. Achieved by: collapsing RhythmSelector from 2 rows to 1, merging NUEVO CICLO + DEFIBRILAR into a shared row, reducing MetronomePulse circle size, reducing TimerCard padding/font, and tightening MainPage spacing.

Purpose: During an emergency, the code leader must access drug buttons instantly without scrolling.
Output: Four modified XAML files with compressed layout, all existing functionality preserved.
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-modificar-frontend-para-mostrar-botones-adrenalina-y-amiodarona-en-primera-vista-con-layout-optimizado/07-CONTEXT.md
@.planning/phases/07-modificar-frontend-para-mostrar-botones-adrenalina-y-amiodarona-en-primera-vista-con-layout-optimizado/07-RESEARCH.md

<interfaces>
<!-- Key references the executor needs -->

From AclsTracker/Views/MainPage.xaml.cs (lines 44-47) — DefibrilarButton x:Name is used for defibrillation animation:
```csharp
await DefibrilarButton.FadeTo(0.3, 100);
await DefibrilarButton.FadeTo(1.0, 200);
await DefibrilarButton.ScaleTo(1.1, 100, Easing.CubicOut);
await DefibrilarButton.ScaleTo(1.0, 150, Easing.CubicIn);
```
CRITICAL: x:Name="DefibrilarButton" MUST be preserved on the DEFIBRILAR button.

From AclsTracker/Controls/MetronomePulse.xaml.cs — Animation uses ScaleTo (multiplicative, NOT absolute):
```csharp
await PulseCircle.ScaleTo(1.15, 80, Easing.CubicOut);
await PulseCircle.ScaleTo(1.0, 120, Easing.CubicIn);
```
Safe to change circle WidthRequest/HeightRequest — no hardcoded pixel references.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compress control files — RhythmSelector, MetronomePulse, TimerCard</name>
  <files>AclsTracker/Controls/RhythmSelector.xaml, AclsTracker/Controls/MetronomePulse.xaml, AclsTracker/Controls/TimerCard.xaml</files>
  <action>
  Edit three XAML control files. No code-behind changes needed.

  **RhythmSelector.xaml — Replace FlexLayout with Grid (single row, ~60dp savings):**
  1. Replace the `<FlexLayout Wrap="Wrap" ...>` block (lines 25-69) with a `<Grid>` using `ColumnDefinitions="*, *, *, *, *"` and `ColumnSpacing="4"`
  2. Place each Button in Grid.Column="0" through Grid.Column="4" (order: RCE, AESP, ASISTOLIA, TV, FV)
  3. On each button: set `FontSize="11"`, `HeightRequest="36"`, `Padding="2,0"`
  4. Remove `MinimumWidthRequest="80"` and `Margin="4"` from all buttons (incompatible with Grid * columns)
  5. Keep all `Command`, `CommandParameter`, `BackgroundColor`, `TextColor`, `FontAttributes`, `CornerRadius` exactly as they are
  6. Reduce parent VerticalStackLayout Spacing from 8 to 6

  **MetronomePulse.xaml — Reduce circle size (~5dp savings):**
  1. Change outer Grid: `WidthRequest="45" HeightRequest="45"` (was 50)
  2. Change Ellipse PulseCircle: `WidthRequest="40" HeightRequest="40"` (was 44)
  3. Do NOT change anything else — animation code uses ScaleTo which is multiplicative

  **TimerCard.xaml — Reduce padding and font (~28dp savings across 6 cards):**
  1. Change Frame `Padding="4"` (was 8)
  2. Change elapsed time Label (Grid.Row="1") `FontSize="20"` (was 24)
  3. Keep Frame Margin="2" — do NOT remove it (visual separation between cards)
  4. Keep all other properties unchanged (triggers, progress bar, pause button, etc.)
  </action>
  <verify>
    <automated>cd /c/Users/Adria/Documents/code/CPRHelper && grep -c "ColumnDefinitions=\"\*, \*, \*, \*, \*\"" AclsTracker/Controls/RhythmSelector.xaml && grep -c "HeightRequest=\"45\"" AclsTracker/Controls/MetronomePulse.xaml && grep -c "Padding=\"4\"" AclsTracker/Controls/TimerCard.xaml</automated>
  </verify>
  <done>RhythmSelector uses Grid with 5 equal * columns, FontSize=11, HeightRequest=36. MetronomePulse circle is 45/40px. TimerCard Padding=4, elapsed FontSize=20. No FlexLayout remains in RhythmSelector.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure MainPage action buttons and reduce spacing</name>
  <files>AclsTracker/Views/MainPage.xaml</files>
  <action>
  Edit MainPage.xaml to merge action buttons into a 2x2 Grid and reduce overall spacing.

  **Action buttons — Replace 3 separate elements with 1 unified Grid (~48dp savings):**
  Replace lines 87-145 (the three action elements: NUEVO CICLO button, DEFIBRILAR button, ADRENALINA+AMIODARONA Grid) with a single Grid:

  ```xml
  <!-- BLOCK 5: Botones de accion — 2 rows x 2 columns -->
  <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto"
        ColumnSpacing="8" RowSpacing="6">
      <!-- Row 0: NUEVO CICLO + DEFIBRILAR -->
      <Button Grid.Row="0" Grid.Column="0"
              Text="NUEVO CICLO"
              Command="{Binding NewCycleCommand}"
              BackgroundColor="#FF9800"
              TextColor="White" FontSize="14" FontAttributes="Bold"
              HeightRequest="40" CornerRadius="8" />
      <Button Grid.Row="0" Grid.Column="1"
              x:Name="DefibrilarButton"
              Text="DEFIBRILAR"
              Command="{Binding DefibrilarCommand}"
              BackgroundColor="#C62828"
              TextColor="White" FontSize="14" FontAttributes="Bold"
              HeightRequest="40" CornerRadius="8" />
      <!-- Row 1: ADRENALINA + AMIODARONA (DataTriggers preserved) -->
      <Button Grid.Row="1" Grid.Column="0"
              Text="ADRENALINA"
              Command="{Binding AdrenalinaCommand}"
              BackgroundColor="#7B1FA2"
              TextColor="White" FontSize="14" FontAttributes="Bold"
              HeightRequest="40" CornerRadius="8">
          <Button.Triggers>
              <DataTrigger TargetType="Button"
                           Binding="{Binding IsAdrenalinaSuggested}"
                           Value="True">
                  <Setter Property="BackgroundColor" Value="#D32F2F" />
              </DataTrigger>
          </Button.Triggers>
      </Button>
      <Button Grid.Row="1" Grid.Column="1"
              Text="AMIODARONA"
              Command="{Binding AmiodaronaCommand}"
              IsEnabled="{Binding IsAmiodaronaEnabled}"
              Opacity="{Binding IsAmiodaronaEnabled, Converter={StaticResource BoolToOpacityConverter}}"
              BackgroundColor="#7B1FA2"
              TextColor="White" FontSize="14" FontAttributes="Bold"
              HeightRequest="40" CornerRadius="8">
          <Button.Triggers>
              <DataTrigger TargetType="Button"
                           Binding="{Binding IsAmiodaronaSuggested}"
                           Value="True">
                  <Setter Property="BackgroundColor" Value="#D32F2F" />
              </DataTrigger>
          </Button.Triggers>
      </Button>
  </Grid>
  ```

  CRITICAL: `x:Name="DefibrilarButton"` MUST be on the DEFIBRILAR button — MainPage.xaml.cs uses it for defibrillation animation (FadeTo, ScaleTo).

  **VerticalStackLayout spacing — Reduce overall spacing (~10dp savings):**
  1. Change `Padding="10"` (was 12) on the VerticalStackLayout
  2. Change `Spacing="6"` (was 8) on the VerticalStackLayout

  **Do NOT change:**
  - INICIAR/FINALIZAR CODIGO block (keep HeightRequest=48)
  - Timer Grid structure (keep ColumnSpacing=6, RowSpacing=6 — per Claude's discretion these are already reasonable)
  - EventLogPanel reference
  - NotificationBanner
  - Any code-behind file
  </action>
  <verify>
    <automated>cd /c/Users/Adria/Documents/code/CPRHelper && grep -c "x:Name=\"DefibrilarButton\"" AclsTracker/Views/MainPage.xaml && grep "Spacing=" AclsTracker/Views/MainPage.xaml | head -2 && grep -c "IsAdrenalinaSuggested" AclsTracker/Views/MainPage.xaml && grep -c "IsAmiodaronaSuggested" AclsTracker/Views/MainPage.xaml && grep -c "BoolToOpacityConverter" AclsTracker/Views/MainPage.xaml</automated>
  </verify>
  <done>Action buttons are in a unified 2x2 Grid. NUEVO CICLO + DEFIBRILAR share row 0, ADRENALINA + AMIODARONA share row 1. x:Name="DefibrilarButton" preserved. DataTriggers for IsAdrenalinaSuggested and IsAmiodaronaSuggested preserved. BoolToOpacityConverter on AMIODARONA preserved. VSL Padding=10, Spacing=6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification on Android emulator</name>
  <files>N/A</files>
  <action>
  Human verifies the compressed layout on a real Android emulator.

  What was built: Compressed MainPage layout — rhythm buttons in single row, action buttons in 2x2 grid, reduced metronome circle, tighter timer cards and spacing. Total vertical savings ~100dp.

  Verification steps:
    1. Deploy to Android emulator (standard ~360x800dp screen)
    2. Open the app — do NOT scroll
    3. Verify ALL of the following are visible without scrolling:
       - INICIAR CODIGO button at top
       - Metronome pulse circle (smaller but functional)
       - All 5 rhythm buttons in ONE row (RCE, AESP, ASISTOLIA, TV, FV) — "ASISTOLIA" should not be clipped
       - 6 timer cards (3 rows x 2 columns)
       - NUEVO CICLO (orange) + DEFIBRILAR (red) side by side
       - ADRENALINA (purple) + AMIODARONA (purple) side by side
    4. Tap INICIAR CODIGO to start a session
    5. Verify metronome pulse animation works (circle scales up/down)
    6. Tap DEFIBRILAR — verify the flash animation plays (fade + scale)
    7. Wait for adrenalina suggestion — verify ADRENALINA button turns red
    8. Verify AMIODARONA appears dimmed (lower opacity) before first adrenalina dose
  </action>
  <verify>Human visual confirmation on Android emulator</verify>
  <done>All layout compressions verified visually: drug buttons visible without scroll, all animations functional, DataTrigger highlights working.</done>
</task>

</tasks>

<verification>
- All 4 XAML files modified, no code-behind changes
- RhythmSelector: Grid with 5 equal * columns, no FlexLayout
- MetronomePulse: circle 45/40px, animation unchanged
- TimerCard: Padding=4, elapsed FontSize=20
- MainPage: unified 2x2 action Grid, x:Name preserved, DataTriggers preserved, BoolToOpacityConverter preserved
- VSL Padding=10, Spacing=6
</verification>

<success_criteria>
ADRENALINA and AMIODARONA buttons are fully visible without scrolling on a standard Android device (~360x800dp). All existing functionality (metronome animation, defibrillation animation, drug suggestion highlights, amiodarona opacity) works unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-modificar-frontend-para-mostrar-botones-adrenalina-y-amiodarona-en-primera-vista-con-layout-optimizado/07-01-SUMMARY.md`
</output>
