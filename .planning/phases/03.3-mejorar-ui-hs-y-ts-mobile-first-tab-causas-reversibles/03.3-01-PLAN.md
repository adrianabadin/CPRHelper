---
phase: 03.3-mejorar-ui-hs-y-ts-mobile-first-tab-causas-reversibles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AclsTracker/Controls/HsAndTsChecklist.xaml
  - AclsTracker/Views/HsAndTsPage.xaml
autonomous: true
requirements: []

must_haves:
  truths:
    - "User sees items in single-column vertical list"
    - "User can tap anywhere on a row to toggle item"
    - "User sees large 48x48 round buttons for check/dismiss"
    - "User sees 16pt font for item names"
    - "Tab displays as 'Causas Reversibles'"
  artifacts:
    - path: "AclsTracker/Controls/HsAndTsChecklist.xaml"
      provides: "HsAndTsChecklist UI with mobile-first improvements"
      contains: "Span=\"1\"", "WidthRequest=\"48\"", "HeightRequest=\"48\"", "CornerRadius=\"18\"", "FontSize=\"16\"", "TapGestureRecognizer", "InputTransparent=\"True\""
    - path: "AclsTracker/Views/HsAndTsPage.xaml"
      provides: "Tab page with renamed title"
      contains: "Title=\"Causas Reversibles\""
  key_links:
    - from: "Frame.GestureRecognizers"
      to: "EventRecordingViewModel.ToggleHsAndTsItemCommand"
      via: "TapGestureRecognizer Command binding"
      pattern: "x:Reference Root.*ToggleHsAndTsItemCommand"
    - from: "Buttons"
      to: "Frame"
      via: "InputTransparent=True allows tap propagation"
      pattern: "InputTransparent=\"True\""
---

<objective>
Improved mobile-first UI for HsAndTsChecklist control with single-column layout, large 48x48 round buttons, full-row tap gesture, and larger 16pt font. Tab renamed to "Causas Reversibles". Functionality unchanged — only presentation layer modified.
</objective>

<execution_context>
@C:/Users/Adria/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AclsTracker/Controls/HsAndTsChecklist.xaml
@AclsTracker/Views/HsAndTsPage.xaml

## Current State (HsAndTsChecklist.xaml)
- GridItemsLayout Span="2" (2 columns)
- Buttons: 36x36, CornerRadius="4", FontSize="14"
- Label FontSize="12"
- No TapGestureRecognizer on Frame
- Section header: "H's y T's — CAUSAS REVERSIBLES" (gray, 12pt)

## Desired State (from CONTEXT.md user decisions)
- GridItemsLayout Span="1" (single column)
- Buttons: 48x48, CornerRadius="18", FontSize="18" 
- Label FontSize="16"
- Frame with TapGestureRecognizer for full-row tap
- Buttons with InputTransparent="True" to allow tap propagation
- Section headers: "H's" and "T's" in blue (#1565C0), 14pt bold
- Top-level header removed (per user decision to have sections only)
- Tab title: "Causas Reversibles" (not "H's y T's")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign H's section item template</name>
  <files>AclsTracker/Controls/HsAndTsChecklist.xaml</files>
  <action>
    In the H's CollectionView section (lines 25-104), modify the item template:

    1. Change GridItemsLayout from Span="2" to Span="1" (line 29)
    
    2. In the Frame element (lines 35-38):
       - Change Padding="8" to Padding="12"
       - Change CornerRadius="8" to CornerRadius="18"
       - Change Margin="2" to Margin="4,6"
       - Add GestureRecognizers inside Frame (after HasShadow="False"):
         ```xml
         <Frame.GestureRecognizers>
             <TapGestureRecognizer 
                 Command="{Binding Source={x:Reference Root}, Path=BindingContext.ToggleHsAndTsItemCommand}"
                 CommandParameter="{Binding .}" />
         </Frame.GestureRecognizers>
         ```

    3. In the Label (lines 59-72):
       - Change FontSize="12" to FontSize="16"

    4. In the check Button (lines 75-85):
       - Change WidthRequest="36" to WidthRequest="48"
       - Change HeightRequest="36" to HeightRequest="48"
       - Change CornerRadius="4" to CornerRadius="18"
       - Change FontSize="14" to FontSize="18"
       - Add InputTransparent="True"

    5. In the dismiss Button (lines 88-98):
       - Change WidthRequest="36" to WidthRequest="48"
       - Change HeightRequest="36" to HeightRequest="48"
       - Change CornerRadius="4" to CornerRadius="18"
       - Change FontSize="14" to FontSize="18"
       - Add InputTransparent="True"

    6. Remove the top-level "H's y T's — CAUSAS REVERSIBLES" header (lines 10-16) since user wants only section headers "H's" and "T's"
  </action>
  <verify>
    Build succeeds: dotnet build AclsTracker/AclsTracker.csproj --no-restore -v q 2>&1 | grep -E "error|Build succeeded"
  </verify>
  <done>H's section items display as single-column, 48x48 round buttons, 16pt font, full-row tappable</done>
</task>

<task type="auto">
  <name>Task 2: Redesign T's section item template</name>
  <files>AclsTracker/Controls/HsAndTsChecklist.xaml</files>
  <action>
    In the T's CollectionView section (lines 113-192), apply the same changes as Task 1:

    1. Change GridItemsLayout from Span="2" to Span="1" (line 117)
    
    2. In the Frame element (lines 123-126):
       - Change Padding="8" to Padding="12"
       - Change CornerRadius="8" to CornerRadius="18"
       - Change Margin="2" to Margin="4,6"
       - Add GestureRecognizers inside Frame:
         ```xml
         <Frame.GestureRecognizers>
             <TapGestureRecognizer 
                 Command="{Binding Source={x:Reference Root}, Path=BindingContext.ToggleHsAndTsItemCommand}"
                 CommandParameter="{Binding .}" />
         </Frame.GestureRecognizers>
         ```

    3. In the Label (lines 147-160):
       - Change FontSize="12" to FontSize="16"

    4. In the check Button (lines 163-173):
       - Change WidthRequest="36" to WidthRequest="48"
       - Change HeightRequest="36" to HeightRequest="48"
       - Change CornerRadius="4" to CornerRadius="18"
       - Change FontSize="14" to FontSize="18"
       - Add InputTransparent="True"

    5. In the dismiss Button (lines 176-186):
       - Change WidthRequest="36" to WidthRequest="48"
       - Change HeightRequest="36" to HeightRequest="48"
       - Change CornerRadius="4" to CornerRadius="18"
       - Change FontSize="14" to FontSize="18"
       - Add InputTransparent="True"
  </action>
  <verify>
    Build succeeds: dotnet build AclsTracker/AclsTracker.csproj --no-restore -v q 2>&1 | grep -E "error|Build succeeded"
  </verify>
  <done>T's section items display as single-column, 48x48 round buttons, 16pt font, full-row tappable</done>
</task>

<task type="auto">
  <name>Task 3: Rename tab to "Causas Reversibles"</name>
  <files>AclsTracker/Views/HsAndTsPage.xaml</files>
  <action>
    In HsAndTsPage.xaml (line 6):
    - Change Title="H's y T's" to Title="Causas Reversibles"
  </action>
  <verify>
    Build succeeds: dotnet build AclsTracker/AclsTracker.csproj --no-restore -v q 2>&1 | grep -E "error|Build succeeded"
  </verify>
  <done>Tab displays as "Causas Reversibles" in navigation</done>
</task>

</tasks>

<verification>
- [ ] Build succeeds without errors
- [ ] HsAndTsChecklist.xaml modified with Span="1" for both CollectionViews
- [ ] Buttons are 48x48 with CornerRadius="18" in both sections
- [ ] Labels use FontSize="16" in both sections
- [ ] TapGestureRecognizer added to Frames in both sections
- [ ] Buttons have InputTransparent="True" in both sections
- [ ] Top-level header removed from HsAndTsChecklist.xaml
- [ ] HsAndTsPage.xaml has Title="Causas Reversibles"
</verification>

<success_criteria>
- Build completes without errors
- Single-column layout in HsAndTsChecklist control
- 48x48 round buttons (CornerRadius=18) for check/dismiss
- 16pt font for item labels
- Full row tappable (TapGestureRecognizer on Frame with InputTransparent on buttons)
- Tab title shows "Causas Reversibles"
- Functionality (check/dismiss commands) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-mejorar-ui-hs-y-ts-mobile-first-tab-causas-reversibles/03.3-01-SUMMARY.md`
</output>