---
phase: 02.1-modificar-la-estructura-de-la-ui-del-proyecto
plan: 03
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified:
  - AclsTracker/Views/MainPage.xaml
  - AclsTracker/Views/MainPage.xaml.cs
  - AclsTracker/ViewModels/MainViewModel.cs
  - AclsTracker/Converters/BoolToOpacityConverter.cs
autonomous: false
requirements:
  - TIME-03
  - REGI-01

must_haves:
  truths:
    - "All blocks 1-5 (session button, metronome, rhythm, timers, action buttons) fit above fold on standard phone screen"
    - "NUEVO CICLO resets CPR cycle and compressions timers, resumes compressions, resets pulse-check"
    - "ADRENALINA resets and starts the epinephrine timer"
    - "AMIODARONA button is only enabled when current rhythm is TV or FV"
    - "2-minute popup fires after NUEVO CICLO, pauses compressions and starts pulse-check timer on accept"
    - "EventLog panel is below fold, scrollable"
    - "HsAndTsChecklist is NOT on MainPage (moved to H's y T's tab)"
  artifacts:
    - path: "AclsTracker/Views/MainPage.xaml"
      provides: "Complete compact MainPage layout with 6 blocks in correct order"
      contains: "NUEVO CICLO"
    - path: "AclsTracker/ViewModels/MainViewModel.cs"
      provides: "2-minute popup countdown, Amiodarona enable logic, action button commands"
      contains: "_pulseCheckTimer"
    - path: "AclsTracker/Converters/BoolToOpacityConverter.cs"
      provides: "Converts bool to 1.0/0.4 opacity for disabled Amiodarona button"
      contains: "BoolToOpacityConverter"
  key_links:
    - from: "AclsTracker/ViewModels/MainViewModel.cs"
      to: "AclsTracker/ViewModels/TimerViewModel.cs"
      via: "Timer.PauseCompressions(), Timer.StartPulseCheckTimer(), Timer.NewCprCycleCommand"
      pattern: "Timer\\.PauseCompressions|Timer\\.StartPulseCheckTimer"
    - from: "AclsTracker/ViewModels/MainViewModel.cs"
      to: "AclsTracker/ViewModels/EventRecordingViewModel.cs"
      via: "EventRecording.PropertyChanged for CurrentRhythm -> IsAmiodaronaEnabled"
      pattern: "IsAmiodaronaEnabled"
    - from: "AclsTracker/Views/MainPage.xaml"
      to: "AclsTracker/ViewModels/MainViewModel.cs"
      via: "Binding commands NewCycleCommand, AdrenalinaCommand, AmiodaronaCommand"
      pattern: "NewCycleCommand|AmiodaronaCommand"
---

<objective>
Redesign MainPage.xaml to the compact above-the-fold layout with all 6 blocks, wire MainViewModel with 2-minute pulse-check popup, action button commands, and Amiodarona conditional enable logic.

Purpose: This is the final integration plan that connects all foundation work (Plan 01 models, Plan 02 controls/pages) into the working MainPage. The user's primary interaction surface during a code.

Output: Complete MainPage with session control, compact metronome, rhythm selector, 6-timer grid, action buttons (NUEVO CICLO, ADRENALINA, AMIODARONA), 2-min popup, and EventLog below fold.
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-CONTEXT.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-RESEARCH.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-01-SUMMARY.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-02-SUMMARY.md

<interfaces>
<!-- From Plans 01 and 02 outputs -->

From AclsTracker/Models/CardiacRhythm.cs:
```csharp
public enum CardiacRhythm { Ninguno, RCE, AESP, Asistolia, TV, FV }
```

From AclsTracker/ViewModels/TimerViewModel.cs (after Plan 01):
```csharp
public ObservableCollection<TimerModel> Timers => _timerService.Timers;
// Timers[0]=Tiempo Total, [1]=Ciclo RCP, [2]=Compresiones, [3]=Adrenalina, [4]=Amiodarona, [5]=T. Pulsos
[ObservableProperty] private bool _isSessionActive;
[RelayCommand] private void StartSession();
[RelayCommand] private void StopSession();
[RelayCommand] private void NewCprCycle();
[RelayCommand] private void MarkMedicationGiven();
[RelayCommand] private void MarkAmiodaronaGiven();
public void PauseCompressions();
public void ResumeCompressions();
public void StartPulseCheckTimer();
public void ResetPulseCheckTimer();
```

From AclsTracker/ViewModels/EventRecordingViewModel.cs:
```csharp
[ObservableProperty] private CardiacRhythm _currentRhythm;
[ObservableProperty] private bool _isSessionActive;
public ObservableCollection<EventRecord> Events => ...;
[RelayCommand] private void SelectRhythm(CardiacRhythm rhythm);
[RelayCommand] private void StartRecording();
[RelayCommand] private void StopRecording();
// PropertyChanged fires when CurrentRhythm changes
```

From AclsTracker/ViewModels/MainViewModel.cs (CURRENT - to be expanded):
```csharp
public MetronomeViewModel Metronome { get; }
public TimerViewModel Timer { get; }
public EventRecordingViewModel EventRecording { get; }
[RelayCommand] private void StartCode();
[RelayCommand] private void StopCode();
```

From AclsTracker/Controls/MetronomePulse.xaml (after Plan 02):
```xml
<!-- Compact horizontal row, binds to Bpm, IsPlaying, DecreaseBpmCommand, ToggleMetronomeCommand, IncreaseBpmCommand -->
```

From AclsTracker/Controls/RhythmSelector.xaml (after Plan 02):
```xml
<!-- 5 buttons: RCE, AESP, ASISTOLIA, TV, FV with SelectRhythmCommand -->
```

From AclsTracker/Converters/BoolToColorConverter.cs:
```csharp
// Already exists in project
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand MainViewModel with 2-minute popup, action commands, and Amiodarona logic</name>
  <files>AclsTracker/ViewModels/MainViewModel.cs, AclsTracker/Converters/BoolToOpacityConverter.cs</files>
  <action>
1. **MainViewModel.cs** - Major expansion. Keep existing constructor DI and Metronome/Timer/EventRecording properties. Add:

   a) **IsAmiodaronaEnabled property** — computed from CurrentRhythm:
   ```csharp
   [ObservableProperty]
   private bool _isAmiodaronaEnabled;
   ```
   In constructor, subscribe to EventRecording.PropertyChanged:
   ```csharp
   EventRecording.PropertyChanged += (_, e) =>
   {
       if (e.PropertyName == nameof(EventRecordingViewModel.CurrentRhythm))
           IsAmiodaronaEnabled = EventRecording.CurrentRhythm is CardiacRhythm.TV or CardiacRhythm.FV;
   };
   ```

   b) **IDispatcherTimer for 2-minute pulse check:**
   ```csharp
   private IDispatcherTimer? _pulseCheckTimer;
   ```

   c) **NewCycleCommand** — wraps Timer.NewCprCycle plus 2-min countdown reset:
   ```csharp
   [RelayCommand]
   private void NewCycle()
   {
       Timer.NewCprCycleCommand.Execute(null);
       EventRecording.LogCustomEventCommand.Execute("Nuevo ciclo RCP");

       // Reset 2-minute countdown
       _pulseCheckTimer?.Stop();
       _pulseCheckTimer = Application.Current!.Dispatcher.CreateTimer();
       _pulseCheckTimer.Interval = TimeSpan.FromMinutes(2);
       _pulseCheckTimer.IsRepeating = false;
       _pulseCheckTimer.Tick += OnPulseCheckDue;
       _pulseCheckTimer.Start();
   }
   ```

   d) **OnPulseCheckDue** — fires popup, pauses compressions, starts pulse-check timer:
   ```csharp
   private async void OnPulseCheckDue(object? sender, EventArgs e)
   {
       _pulseCheckTimer?.Stop();
       await Application.Current!.MainPage!
           .DisplayAlert("Check de Pulso", "Han pasado 2 minutos. Constate pulso y ritmo.", "ACEPTAR");
       // After ACEPTAR:
       Timer.PauseCompressions();
       Timer.StartPulseCheckTimer();
   }
   ```

   e) **AdrenalinaCommand:**
   ```csharp
   [RelayCommand]
   private void Adrenalina()
   {
       Timer.MarkMedicationGivenCommand.Execute(null);
       EventRecording.LogCustomEventCommand.Execute("Adrenalina administrada");
   }
   ```

   f) **AmiodaronaCommand:**
   ```csharp
   [RelayCommand]
   private void Amiodarona()
   {
       Timer.MarkAmiodaronaGivenCommand.Execute(null);
       EventRecording.LogCustomEventCommand.Execute("Amiodarona administrada");
   }
   ```

   g) **Update StartCode** to also start the 2-min countdown and compressions timer:
   ```csharp
   [RelayCommand]
   private void StartCode()
   {
       Timer.StartSessionCommand.Execute(null);
       EventRecording.StartRecordingCommand.Execute(null);
       // Start compressions timer
       Timer.StartTimerCommand.Execute("compressions");
       // Start 2-minute countdown
       _pulseCheckTimer?.Stop();
       _pulseCheckTimer = Application.Current!.Dispatcher.CreateTimer();
       _pulseCheckTimer.Interval = TimeSpan.FromMinutes(2);
       _pulseCheckTimer.IsRepeating = false;
       _pulseCheckTimer.Tick += OnPulseCheckDue;
       _pulseCheckTimer.Start();
   }
   ```

   h) **Update StopCode** to clean up timer:
   ```csharp
   [RelayCommand]
   private void StopCode()
   {
       _pulseCheckTimer?.Stop();
       _pulseCheckTimer = null;
       Timer.StopSessionCommand.Execute(null);
       EventRecording.StopRecordingCommand.Execute(null);
   }
   ```

   Add required using:
   ```csharp
   using AclsTracker.Models;
   ```

2. **Create AclsTracker/Converters/BoolToOpacityConverter.cs:**
   ```csharp
   using System.Globalization;
   namespace AclsTracker.Converters;
   public class BoolToOpacityConverter : IValueConverter
   {
       public object Convert(object? value, Type targetType, object? parameter, CultureInfo culture)
           => value is true ? 1.0 : 0.4;
       public object ConvertBack(object? value, Type targetType, object? parameter, CultureInfo culture)
           => throw new NotSupportedException();
   }
   ```
  </action>
  <verify>
    <automated>cd C:/Users/Adria/Documents/code/CPRHelper && grep -c "_pulseCheckTimer" AclsTracker/ViewModels/MainViewModel.cs && grep -c "RelayCommand" AclsTracker/ViewModels/MainViewModel.cs && test -f AclsTracker/Converters/BoolToOpacityConverter.cs && echo "exists"</automated>
  </verify>
  <done>MainViewModel has: NewCycleCommand (resets timers + 2-min countdown), AdrenalinaCommand, AmiodaronaCommand, IsAmiodaronaEnabled (reactive to CurrentRhythm), OnPulseCheckDue (DisplayAlert + pause compressions + start pulse-check), StartCode starts compressions + 2-min countdown, StopCode cleans up. BoolToOpacityConverter exists.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign MainPage.xaml — compact above-the-fold layout with 6 blocks</name>
  <files>AclsTracker/Views/MainPage.xaml, AclsTracker/Views/MainPage.xaml.cs</files>
  <action>
Rewrite MainPage.xaml completely following the user's locked layout order. Remove HsAndTsChecklist (moved to tab). The layout MUST fit blocks 1-5 above the fold on a standard ~844pt height phone.

Use ScrollView wrapping a VerticalStackLayout (not Grid) for simpler vertical flow. Use tight spacing (8px between sections) and reduced padding (12px page padding).

```xml
<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:AclsTracker.Controls"
             xmlns:vm="clr-namespace:AclsTracker.ViewModels"
             xmlns:converters="clr-namespace:AclsTracker.Converters"
             x:Class="AclsTracker.Views.MainPage"
             Title="Principal"
             BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#121212}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
            <converters:IsNotNullConverter x:Key="IsNotNullConverter" />
            <converters:InvertBoolConverter x:Key="InvertBoolConverter" />
            <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="12" Spacing="8">

            <!-- BLOCK 1: INICIAR / FINALIZAR CODIGO -->
            <Grid HorizontalOptions="Fill">
                <Button Text="INICIAR CODIGO"
                        Command="{Binding StartCodeCommand}"
                        BackgroundColor="#D32F2F"
                        TextColor="White"
                        FontSize="16" FontAttributes="Bold"
                        HeightRequest="48"
                        CornerRadius="8"
                        HorizontalOptions="Fill"
                        IsVisible="{Binding Timer.IsSessionActive, Converter={StaticResource InvertBoolConverter}}" />
                <Button Text="FINALIZAR CODIGO"
                        Command="{Binding StopCodeCommand}"
                        BackgroundColor="#757575"
                        TextColor="White"
                        FontSize="16" FontAttributes="Bold"
                        HeightRequest="48"
                        CornerRadius="8"
                        HorizontalOptions="Fill"
                        IsVisible="{Binding Timer.IsSessionActive}" />
            </Grid>

            <!-- BLOCK 2: Metronomo compacto horizontal -->
            <controls:MetronomePulse IsPulsing="{Binding Metronome.BeatPulse}"
                                     BindingContext="{Binding Metronome}" />

            <!-- BLOCK 3: Selector de ritmo -->
            <controls:RhythmSelector BindingContext="{Binding EventRecording}" />

            <!-- BLOCK 4: Grid de 6 timers (2 columnas x 3 filas) -->
            <Grid ColumnDefinitions="*, *"
                  RowDefinitions="Auto, Auto, Auto"
                  ColumnSpacing="6" RowSpacing="6">
                <!-- [0] Tiempo Total -->
                <controls:TimerCard Grid.Row="0" Grid.Column="0"
                                    BindingContext="{Binding Timer.Timers[0]}" />
                <!-- [1] Ciclo RCP -->
                <controls:TimerCard Grid.Row="0" Grid.Column="1"
                                    BindingContext="{Binding Timer.Timers[1]}" />
                <!-- [2] Compresiones -->
                <controls:TimerCard Grid.Row="1" Grid.Column="0"
                                    BindingContext="{Binding Timer.Timers[2]}" />
                <!-- [3] Adrenalina -->
                <controls:TimerCard Grid.Row="1" Grid.Column="1"
                                    BindingContext="{Binding Timer.Timers[3]}" />
                <!-- [4] Amiodarona -->
                <controls:TimerCard Grid.Row="2" Grid.Column="0"
                                    BindingContext="{Binding Timer.Timers[4]}" />
                <!-- [5] Tiempo entre pulsos (red > 10s via IsOverThreshold) -->
                <controls:TimerCard Grid.Row="2" Grid.Column="1"
                                    BindingContext="{Binding Timer.Timers[5]}" />
            </Grid>

            <!-- BLOCK 5: Botones de accion -->
            <!-- Row 1: NUEVO CICLO full width -->
            <Button Text="NUEVO CICLO"
                    Command="{Binding NewCycleCommand}"
                    BackgroundColor="#FF9800"
                    TextColor="White"
                    FontSize="15" FontAttributes="Bold"
                    HeightRequest="44"
                    CornerRadius="8"
                    HorizontalOptions="Fill" />

            <!-- Row 2: ADRENALINA + AMIODARONA side by side -->
            <Grid ColumnDefinitions="*, *" ColumnSpacing="8">
                <Button Grid.Column="0"
                        Text="ADRENALINA"
                        Command="{Binding AdrenalinaCommand}"
                        BackgroundColor="#7B1FA2"
                        TextColor="White"
                        FontSize="14" FontAttributes="Bold"
                        HeightRequest="44"
                        CornerRadius="8" />
                <Button Grid.Column="1"
                        Text="AMIODARONA"
                        Command="{Binding AmiodaronaCommand}"
                        IsEnabled="{Binding IsAmiodaronaEnabled}"
                        Opacity="{Binding IsAmiodaronaEnabled, Converter={StaticResource BoolToOpacityConverter}}"
                        BackgroundColor="#7B1FA2"
                        TextColor="White"
                        FontSize="14" FontAttributes="Bold"
                        HeightRequest="44"
                        CornerRadius="8" />
            </Grid>

            <!-- BLOCK 6: EventLog below the fold (scrollable within page scroll) -->
            <controls:EventLogPanel BindingContext="{Binding EventRecording}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
```

**Height budget verification (approximate):**
- Block 1 (session button): 48px + 8 spacing = 56
- Block 2 (metronome compact): ~56px + 8 = 64
- Block 3 (rhythm selector): ~60px + 8 = 68
- Block 4 (timer grid 3 rows): ~240px + 8 = 248
- Block 5 (action buttons 2 rows): 44+44+8+8 = 104
- Page padding: 12+12 = 24
- Tab bar bottom: ~50
- Status bar top: ~44
- **Total: ~658px** — fits within 844pt with ~186px margin

**MainPage.xaml.cs** - Verify the existing code-behind. It should only have the constructor with DI injection. If there is any metronome toggle text logic, keep it. No other changes expected to code-behind.

IMPORTANT: Remove the HsAndTsChecklist from MainPage entirely (it is now in HsAndTsPage tab).
  </action>
  <verify>
    <automated>cd C:/Users/Adria/Documents/code/CPRHelper && grep -c "TimerCard" AclsTracker/Views/MainPage.xaml && grep -c "HsAndTsChecklist" AclsTracker/Views/MainPage.xaml && grep "NUEVO CICLO\|ADRENALINA\|AMIODARONA" AclsTracker/Views/MainPage.xaml | wc -l</automated>
  </verify>
  <done>MainPage has 6 TimerCards (2x3 grid), 0 HsAndTsChecklist references, 3 action buttons (NUEVO CICLO, ADRENALINA, AMIODARONA). AMIODARONA has IsEnabled+Opacity bindings to IsAmiodaronaEnabled. Layout uses compact spacing (12px padding, 8px between sections). All bindings point to correct MainViewModel commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">  <name>Task 3: Visual verification of complete UI restructuring</name>  <files>AclsTracker/Views/MainPage.xaml</files>  <action>Build and run the app. Verify the complete UI restructuring visually:1. Build: `dotnet build AclsTracker/AclsTracker.csproj -f net9.0-windows10.0.19041.0` (or Android target)2. Verify 3 tabs visible at bottom: "Principal", "H's y T's", "Historial"3. On Principal tab:   a. Press INICIAR CODIGO - verify timers start   b. Verify compact metronome row (small circle + BPM + buttons) fits in one line   c. Verify 5 rhythm buttons: RCE (green), AESP (orange), ASISTOLIA (orange), TV (red), FV (red)   d. Verify 6 timer cards in 2x3 grid   e. Verify AMIODARONA button disabled by default, enabled when TV/FV selected   f. Press NUEVO CICLO - verify Ciclo RCP resets   g. Verify all blocks 1-5 fit on screen without scrolling4. Switch to "H's y T's" tab - verify checklist works with shared state5. Switch to "Historial" tab - verify logged events appear6. (Optional) Wait 2 minutes after NUEVO CICLO to verify popup  </action>  <verify>User confirms all visual checks pass or describes issues</verify>  <done>User types "approved" confirming 3-tab navigation, compact layout, action buttons, and timer coordination all work correctly on device</done></task>

</tasks>

<verification>
- Build compiles without errors
- 3-tab navigation works
- All blocks 1-5 visible above fold on standard phone
- AMIODARONA conditional enable works (TV/FV = enabled, others = disabled)
- 2-minute popup fires and pauses compressions + starts pulse-check timer
- EventLog visible below fold
- H's y T's tab shares state with MainPage
- Historial tab shows session events
</verification>

<success_criteria>
- All 6 blocks in correct order on Principal tab: session control, metronome, rhythm, timers, actions, event log
- Blocks 1-5 above fold on standard phone (~844pt)
- AMIODARONA enabled only when rhythm = TV or FV
- 2-minute popup fires correctly, coordinates timer state
- 3 tabs functional: Principal, H's y T's, Historial
- Zero references to HsAndTsChecklist on MainPage
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-03-SUMMARY.md`
</output>
