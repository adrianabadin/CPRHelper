---
phase: 02.1-modificar-la-estructura-de-la-ui-del-proyecto
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - AclsTracker/Controls/MetronomePulse.xaml
  - AclsTracker/Controls/RhythmSelector.xaml
  - AclsTracker/Views/HsAndTsPage.xaml
  - AclsTracker/Views/HsAndTsPage.xaml.cs
  - AclsTracker/Views/HistorialPage.xaml
  - AclsTracker/Views/HistorialPage.xaml.cs
  - AclsTracker/AppShell.xaml
  - AclsTracker/MauiProgram.cs
autonomous: true
requirements:
  - AUDI-02
  - REGI-01
  - REGI-02
  - REGI-03

must_haves:
  truths:
    - "MetronomePulse is a single horizontal row with 50px circle, BPM label, and controls"
    - "RhythmSelector shows exactly 5 buttons: RCE (green), AESP (orange), ASISTOLIA (orange), TV (red), FV (red)"
    - "App has 3 tabs: Principal, H's y T's, Historial"
    - "H's y T's tab shows the existing HsAndTsChecklist control with shared state"
    - "Historial tab shows read-only event list from IEventLogService"
  artifacts:
    - path: "AclsTracker/Controls/MetronomePulse.xaml"
      provides: "Compact horizontal metronome row (50px circle)"
      contains: "HorizontalStackLayout"
    - path: "AclsTracker/Controls/RhythmSelector.xaml"
      provides: "5-button rhythm selector with updated enum values and colors"
      contains: "CardiacRhythm.RCE"
    - path: "AclsTracker/Views/HsAndTsPage.xaml"
      provides: "Tab page wrapping HsAndTsChecklist control"
      contains: "HsAndTsChecklist"
    - path: "AclsTracker/Views/HistorialPage.xaml"
      provides: "Read-only event list page"
      contains: "CollectionView"
    - path: "AclsTracker/AppShell.xaml"
      provides: "3-tab Shell navigation"
      contains: "TabBar"
  key_links:
    - from: "AclsTracker/Views/HsAndTsPage.xaml.cs"
      to: "AclsTracker/ViewModels/EventRecordingViewModel.cs"
      via: "DI constructor injection, BindingContext = vm"
      pattern: "EventRecordingViewModel"
    - from: "AclsTracker/Views/HistorialPage.xaml.cs"
      to: "AclsTracker/Services/EventLog/IEventLogService.cs"
      via: "DI constructor injection, BindingContext = eventLogService"
      pattern: "IEventLogService"
    - from: "AclsTracker/MauiProgram.cs"
      to: "AclsTracker/Views/HsAndTsPage.xaml.cs"
      via: "AddTransient<HsAndTsPage>()"
      pattern: "AddTransient.*HsAndTsPage"
---

<objective>
Redesign MetronomePulse to compact horizontal layout, update RhythmSelector buttons to new 5-rhythm enum, create HsAndTsPage and HistorialPage, and convert AppShell to 3-tab navigation.

Purpose: All UI controls and navigation structure must be in place before Plan 03 wires the complete MainPage layout with action buttons and popup logic.

Output: Compact MetronomePulse, updated RhythmSelector, two new pages, 3-tab Shell, and DI registrations.
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-CONTEXT.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-RESEARCH.md
@.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (updated enums) -->

From AclsTracker/Models/CardiacRhythm.cs (after Plan 01):
```csharp
public enum CardiacRhythm { Ninguno, RCE, AESP, Asistolia, TV, FV }
```

From AclsTracker/ViewModels/EventRecordingViewModel.cs:
```csharp
public partial class EventRecordingViewModel : ObservableObject
{
    [ObservableProperty] private CardiacRhythm _currentRhythm;
    [ObservableProperty] private bool _isSessionActive;
    public ObservableCollection<EventRecord> Events => _eventLogService.Events;
    public ObservableCollection<HsAndTsItem> HsAndTsItems { get; }
    public IEnumerable<HsAndTsItem> HItems => ...;
    public IEnumerable<HsAndTsItem> TItems => ...;
    [RelayCommand] private void SelectRhythm(CardiacRhythm rhythm);
    [RelayCommand] private void ToggleHsAndTsItem(HsAndTsItem item);
    [RelayCommand] private void DismissHsAndTsItem(HsAndTsItem item);
}
```

From AclsTracker/Services/EventLog/IEventLogService.cs:
```csharp
public interface IEventLogService
{
    ObservableCollection<EventRecord> Events { get; }
    DateTime? SessionStartTime { get; }
    void StartSession(); void EndSession();
    EventRecord LogEvent(string eventType, string description, string? details = null);
    void ClearEvents();
}
```

From AclsTracker/Models/EventRecord.cs:
```csharp
public partial class EventRecord : ObservableObject
{
    public string Id { get; init; }
    public DateTime Timestamp { get; init; }
    public TimeSpan ElapsedSinceStart { get; init; }
    public string EventType { get; init; }
    public string Description { get; init; }
    public string? Details { get; init; }
}
```

From AclsTracker/Controls/MetronomePulse.xaml.cs:
```csharp
public partial class MetronomePulse : ContentView
{
    public static readonly BindableProperty IsPulsingProperty = ...;
    public bool IsPulsing { get; set; }
    // AnimatePulse() targets PulseCircle by x:Name
}
```

From AclsTracker/ViewModels/MetronomeViewModel.cs:
```csharp
// Exposes: Bpm, IsPlaying, BeatPulse, BeatCount
// Commands: ToggleMetronomeCommand, IncreaseBpmCommand, DecreaseBpmCommand
```

From AclsTracker/MauiProgram.cs (current registrations):
```csharp
builder.Services.AddTransient<EventRecordingViewModel>();
builder.Services.AddTransient<MainViewModel>();
builder.Services.AddTransient<MainPage>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign MetronomePulse to compact horizontal layout and update RhythmSelector</name>
  <files>AclsTracker/Controls/MetronomePulse.xaml, AclsTracker/Controls/RhythmSelector.xaml</files>
  <action>
1. **MetronomePulse.xaml** - Replace the entire layout with a compact horizontal row. The code-behind (`AnimatePulse()` targeting `PulseCircle` by x:Name) must still work, so preserve the `x:Name="PulseCircle"` on the Ellipse.

   Replace the content with:
   ```xml
   <HorizontalStackLayout Spacing="12" VerticalOptions="Center" HorizontalOptions="Center" Padding="8,4">
       <!-- Compact pulse circle 50px -->
       <Grid WidthRequest="50" HeightRequest="50">
           <Ellipse x:Name="PulseCircle"
                    WidthRequest="44" HeightRequest="44"
                    Fill="{StaticResource Primary}"
                    Opacity="0.8"
                    HorizontalOptions="Center"
                    VerticalOptions="Center" />
       </Grid>
       <!-- BPM large label -->
       <Label Text="{Binding Bpm}"
              FontSize="30"
              FontAttributes="Bold"
              VerticalOptions="Center"
              TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
              MinimumWidthRequest="40" />
       <Label Text="BPM"
              FontSize="12"
              VerticalOptions="End"
              TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
              Margin="0,0,8,6" />
       <!-- Controls: -, ON/STOP, + -->
       <Button Text="&#x2212;"
               Command="{Binding DecreaseBpmCommand}"
               FontSize="20"
               WidthRequest="44" HeightRequest="44"
               CornerRadius="22"
               BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"
               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
               Padding="0" />
       <Button x:Name="ToggleBtn"
               Text="{Binding IsPlaying, StringFormat='{0}'}"
               Command="{Binding ToggleMetronomeCommand}"
               FontSize="14" FontAttributes="Bold"
               WidthRequest="70" HeightRequest="44"
               CornerRadius="8"
               BackgroundColor="{AppThemeBinding Light=#1976D2, Dark=#1565C0}"
               TextColor="White"
               Padding="0" />
       <Button Text="+"
               Command="{Binding IncreaseBpmCommand}"
               FontSize="20"
               WidthRequest="44" HeightRequest="44"
               CornerRadius="22"
               BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"
               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}"
               Padding="0" />
   </HorizontalStackLayout>
   ```

   Remove the `OuterRing` ellipse, the `BpmLabel` and "BPM" labels from the old centered grid, and the VerticalStackLayout wrapper. The old 180x180 grid is gone.

   NOTE: The ToggleBtn text still uses the placeholder binding from before. The code-behind sets text to "ON"/"STOP" based on IsPlaying -- this behavior should remain unchanged. If code-behind does not handle this, the Text should be hardcoded to "ON" and toggled. Check `MetronomePulse.xaml.cs` and `MainPage.xaml.cs` for any text-setting logic. If none exists, bind directly: remove the StringFormat and just use a converter or leave as is (Plan 03 MainPage will handle the toggle button text).

2. **RhythmSelector.xaml** - Replace all 6 buttons with exactly 5 buttons using the new CardiacRhythm enum values per user decision:

   Replace the FlexLayout contents with:
   ```xml
   <!-- RCE - green -->
   <Button Text="RCE"
           Command="{Binding SelectRhythmCommand}"
           CommandParameter="{x:Static models:CardiacRhythm.RCE}"
           BackgroundColor="#2E7D32"
           TextColor="White" FontSize="14" FontAttributes="Bold"
           MinimumWidthRequest="80" HeightRequest="44" CornerRadius="8" Margin="4" />
   <!-- AESP - orange -->
   <Button Text="AESP"
           Command="{Binding SelectRhythmCommand}"
           CommandParameter="{x:Static models:CardiacRhythm.AESP}"
           BackgroundColor="#E65100"
           TextColor="White" FontSize="14" FontAttributes="Bold"
           MinimumWidthRequest="80" HeightRequest="44" CornerRadius="8" Margin="4" />
   <!-- ASISTOLIA - orange -->
   <Button Text="ASISTOLIA"
           Command="{Binding SelectRhythmCommand}"
           CommandParameter="{x:Static models:CardiacRhythm.Asistolia}"
           BackgroundColor="#E65100"
           TextColor="White" FontSize="14" FontAttributes="Bold"
           MinimumWidthRequest="80" HeightRequest="44" CornerRadius="8" Margin="4" />
   <!-- TV - red -->
   <Button Text="TV"
           Command="{Binding SelectRhythmCommand}"
           CommandParameter="{x:Static models:CardiacRhythm.TV}"
           BackgroundColor="#D32F2F"
           TextColor="White" FontSize="14" FontAttributes="Bold"
           MinimumWidthRequest="80" HeightRequest="44" CornerRadius="8" Margin="4" />
   <!-- FV - red -->
   <Button Text="FV"
           Command="{Binding SelectRhythmCommand}"
           CommandParameter="{x:Static models:CardiacRhythm.FV}"
           BackgroundColor="#D32F2F"
           TextColor="White" FontSize="14" FontAttributes="Bold"
           MinimumWidthRequest="80" HeightRequest="44" CornerRadius="8" Margin="4" />
   ```

   Keep the "RITMO CARDIACO" header label and "Current rhythm display" label. Remove the old Bradicardia and Taquicardia buttons entirely.
  </action>
  <verify>
    <automated>cd C:/Users/Adria/Documents/code/CPRHelper && grep -c "HorizontalStackLayout" AclsTracker/Controls/MetronomePulse.xaml && grep -c "CardiacRhythm\." AclsTracker/Controls/RhythmSelector.xaml</automated>
  </verify>
  <done>MetronomePulse is a horizontal row with 50px circle + BPM + controls (no 180px grid). RhythmSelector has exactly 5 buttons bound to RCE, AESP, Asistolia, TV, FV with correct colors (green/orange/red). Zero references to old enum values in XAML.</done>
</task>

<task type="auto">
  <name>Task 2: Create HsAndTsPage, HistorialPage, update AppShell to 3 tabs, register in DI</name>
  <files>AclsTracker/Views/HsAndTsPage.xaml, AclsTracker/Views/HsAndTsPage.xaml.cs, AclsTracker/Views/HistorialPage.xaml, AclsTracker/Views/HistorialPage.xaml.cs, AclsTracker/AppShell.xaml, AclsTracker/MauiProgram.cs</files>
  <action>
1. **Create AclsTracker/Views/HsAndTsPage.xaml:**
   ```xml
   <?xml version="1.0" encoding="utf-8" ?>
   <ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:controls="clr-namespace:AclsTracker.Controls"
                x:Class="AclsTracker.Views.HsAndTsPage"
                Title="H's y T's">
       <ScrollView>
           <controls:HsAndTsChecklist Margin="16" />
       </ScrollView>
   </ContentPage>
   ```

2. **Create AclsTracker/Views/HsAndTsPage.xaml.cs:**
   ```csharp
   using AclsTracker.ViewModels;
   namespace AclsTracker.Views;
   public partial class HsAndTsPage : ContentPage
   {
       public HsAndTsPage(EventRecordingViewModel vm)
       {
           InitializeComponent();
           BindingContext = vm;
       }
   }
   ```
   NOTE: This page needs the SAME EventRecordingViewModel instance as MainPage to share HsAndTsItems state. This requires changing EventRecordingViewModel from AddTransient to AddSingleton in MauiProgram.cs (step 5).

3. **Create AclsTracker/Views/HistorialPage.xaml:**
   ```xml
   <?xml version="1.0" encoding="utf-8" ?>
   <ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:models="clr-namespace:AclsTracker.Models"
                x:Class="AclsTracker.Views.HistorialPage"
                Title="Historial">
       <CollectionView ItemsSource="{Binding Events}" Margin="16">
           <CollectionView.ItemTemplate>
               <DataTemplate x:DataType="models:EventRecord">
                   <Grid ColumnDefinitions="Auto, Auto, *" Padding="0,4">
                       <Label Grid.Column="0"
                              Text="{Binding ElapsedSinceStart, StringFormat='{0:mm\\:ss}'}"
                              FontSize="12" TextColor="#999999" MinimumWidthRequest="50" />
                       <Label Grid.Column="1"
                              Text="{Binding EventType}"
                              FontSize="11" TextColor="#1565C0"
                              MinimumWidthRequest="80" Margin="4,0" />
                       <Label Grid.Column="2"
                              Text="{Binding Description}"
                              FontSize="13" />
                   </Grid>
               </DataTemplate>
           </CollectionView.ItemTemplate>
           <CollectionView.EmptyView>
               <Label Text="No hay eventos registrados en esta sesion."
                      HorizontalTextAlignment="Center"
                      VerticalTextAlignment="Center"
                      TextColor="#999999"
                      FontSize="14"
                      Margin="0,40" />
           </CollectionView.EmptyView>
       </CollectionView>
   </ContentPage>
   ```

4. **Create AclsTracker/Views/HistorialPage.xaml.cs:**
   ```csharp
   using AclsTracker.Services.EventLog;
   namespace AclsTracker.Views;
   public partial class HistorialPage : ContentPage
   {
       public HistorialPage(IEventLogService eventLogService)
       {
           InitializeComponent();
           BindingContext = eventLogService;
       }
   }
   ```
   Injects IEventLogService directly (singleton) rather than EventRecordingViewModel per Research pitfall #4.

5. **Update AclsTracker/MauiProgram.cs:**
   - Change `AddTransient<EventRecordingViewModel>()` to `AddSingleton<EventRecordingViewModel>()` so HsAndTsPage and MainPage share the same instance.
   - Add `builder.Services.AddTransient<HsAndTsPage>();`
   - Add `builder.Services.AddTransient<HistorialPage>();`

6. **Update AclsTracker/AppShell.xaml:**
   Add `xmlns:views` namespace if not present. Replace the single Tab with 3 tabs. Omit Icon attributes (text-only tabs per Research recommendation).
   ```xml
   <Shell ...
          xmlns:views="clr-namespace:AclsTracker.Views"
          Shell.FlyoutBehavior="Disabled">
       <TabBar>
           <Tab Title="Principal">
               <ShellContent ContentTemplate="{DataTemplate views:MainPage}" Route="MainPage" />
           </Tab>
           <Tab Title="H's y T's">
               <ShellContent ContentTemplate="{DataTemplate views:HsAndTsPage}" Route="HsAndTsPage" />
           </Tab>
           <Tab Title="Historial">
               <ShellContent ContentTemplate="{DataTemplate views:HistorialPage}" Route="HistorialPage" />
           </Tab>
       </TabBar>
   </Shell>
   ```
  </action>
  <verify>
    <automated>cd C:/Users/Adria/Documents/code/CPRHelper && test -f AclsTracker/Views/HsAndTsPage.xaml && test -f AclsTracker/Views/HistorialPage.xaml && grep -c "Tab Title=" AclsTracker/AppShell.xaml && grep -c "AddTransient.*Page" AclsTracker/MauiProgram.cs && grep "AddSingleton.*EventRecordingViewModel" AclsTracker/MauiProgram.cs | wc -l</automated>
  </verify>
  <done>HsAndTsPage wraps existing HsAndTsChecklist with shared EventRecordingViewModel (singleton). HistorialPage shows read-only CollectionView of Events from IEventLogService. AppShell has 3 tabs (Principal, H's y T's, Historial). All pages registered in MauiProgram.cs DI container. EventRecordingViewModel is singleton.</done>
</task>

</tasks>

<verification>
- MetronomePulse height reduced from 180px to ~56px (single row)
- RhythmSelector has 5 buttons with correct colors matching user decisions
- 3 tabs visible and navigable in AppShell
- HsAndTsPage shows same checklist state as MainPage
- HistorialPage shows events logged during the session
- No old enum references in any XAML file
</verification>

<success_criteria>
- MetronomePulse is a compact horizontal row (50px circle + BPM + 3 buttons)
- RhythmSelector buttons: RCE (green), AESP (orange), ASISTOLIA (orange), TV (red), FV (red)
- AppShell has 3 tabs: Principal, H's y T's, Historial
- HsAndTsPage and HistorialPage created, registered in DI
- EventRecordingViewModel changed to singleton for shared state
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-modificar-la-estructura-de-la-ui-del-proyecto/02.1-02-SUMMARY.md`
</output>
