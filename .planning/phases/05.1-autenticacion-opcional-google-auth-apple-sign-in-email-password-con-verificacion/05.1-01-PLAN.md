---
phase: 05.1-autenticacion-opcional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AclsTracker/AclsTracker.csproj
  - AclsTracker/Services/Auth/IAuthService.cs
  - AclsTracker/Services/Auth/SupabaseSessionHandler.cs
  - AclsTracker/Models/UserProfile.cs
  - AclsTracker/Constants/SupabaseConfig.cs
  - AclsTracker/Platforms/Android/WebAuthenticationCallbackActivity.cs
  - AclsTracker/Platforms/Android/AndroidManifest.xml
  - AclsTracker/Platforms/iOS/Info.plist
  - AclsTracker/Platforms/iOS/Entitlements.plist
autonomous: true
requirements:
  - AUTH-INFRA
  - AUTH-SESSION

must_haves:
  truths:
    - "Supabase NuGet package is installed and project builds"
    - "IAuthService interface defines all auth operations needed by the app"
    - "Session persistence handler encrypts JWT tokens in SecureStorage"
    - "Android deep-link callback activity handles aclstracker:// scheme"
    - "iOS Info.plist registers aclstracker:// URL scheme"
  artifacts:
    - path: "AclsTracker/Services/Auth/IAuthService.cs"
      provides: "Auth service contract for login, logout, register, OAuth, profile, password reset"
      exports: ["IAuthService"]
    - path: "AclsTracker/Services/Auth/SupabaseSessionHandler.cs"
      provides: "Session persistence using SecureStorage"
      exports: ["SupabaseSessionHandler"]
    - path: "AclsTracker/Models/UserProfile.cs"
      provides: "Profile model matching Supabase profiles table"
      exports: ["UserProfile"]
    - path: "AclsTracker/Constants/SupabaseConfig.cs"
      provides: "Supabase URL and anon key constants"
      exports: ["SupabaseConfig"]
    - path: "AclsTracker/Platforms/Android/WebAuthenticationCallbackActivity.cs"
      provides: "Android OAuth callback handler"
  key_links:
    - from: "SupabaseSessionHandler"
      to: "SecureStorage"
      via: "MAUI SecureStorage API"
      pattern: "SecureStorage\\.Default"
---

<objective>
Set up Supabase auth infrastructure: install NuGet package, define IAuthService interface, implement session persistence with SecureStorage, create UserProfile model, configure platform-specific OAuth callbacks (Android intent filter, iOS URL scheme + Apple entitlement), and create Supabase config constants.

Purpose: Establish the contracts and platform configuration that all subsequent auth plans depend on. No UI or service implementation yet — just foundations.
Output: IAuthService.cs, SupabaseSessionHandler.cs, UserProfile.cs, SupabaseConfig.cs, platform configs
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-autenticacion-opcional-google-auth-apple-sign-in-email-password-con-verificacion/05.1-CONTEXT.md
@.planning/phases/05.1-autenticacion-opcional-google-auth-apple-sign-in-email-password-con-verificacion/05.1-RESEARCH.md

<interfaces>
<!-- Existing DI pattern from MauiProgram.cs -->
From AclsTracker/MauiProgram.cs:
```csharp
// Services registered as singletons
builder.Services.AddSingleton<IAudioService, AudioService>();
builder.Services.AddSingleton<IEventLogService, EventLogService>();
// ViewModels registered as transient
builder.Services.AddTransient<MainViewModel>();
```

From AclsTracker/App.xaml.cs:
```csharp
public partial class App : Application
{
    public App()
    {
        InitializeComponent();
    }
    protected override Window CreateWindow(IActivationState? activationState)
    {
        return new Window(new AppShell());
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Supabase NuGet + define contracts and models</name>
  <files>
    AclsTracker/AclsTracker.csproj,
    AclsTracker/Services/Auth/IAuthService.cs,
    AclsTracker/Models/UserProfile.cs,
    AclsTracker/Constants/SupabaseConfig.cs
  </files>
  <action>
1. Add `Supabase` NuGet package to AclsTracker.csproj:
   ```
   dotnet add AclsTracker/AclsTracker.csproj package Supabase
   ```

2. Create `AclsTracker/Constants/SupabaseConfig.cs`:
   - Static class with `Url` and `AnonKey` string constants
   - Use placeholder values: `"YOUR_SUPABASE_URL"` and `"YOUR_SUPABASE_ANON_KEY"`
   - Add `RedirectUri = "aclstracker://callback"` constant
   - Add XML comment noting these are safe to embed in client (anon key is public by design)

3. Create `AclsTracker/Models/UserProfile.cs`:
   - Properties: `Id` (string, maps to UUID), `Email` (string), `Nombre` (string), `Apellido` (string), `Telefono` (string), `AvatarUrl` (string?), `UpdatedAt` (DateTime?), `CreatedAt` (DateTime?)
   - Use Supabase `[Column]` attribute if available, otherwise plain POCO
   - Add `FullName` computed property returning `$"{Nombre} {Apellido}".Trim()`

4. Create `AclsTracker/Services/Auth/IAuthService.cs`:
   - `Task<bool> SignInWithEmailAsync(string email, string password)`
   - `Task<bool> SignUpWithEmailAsync(string email, string password, string nombre, string apellido, string telefono)`
   - `Task<bool> SignInWithGoogleAsync()`
   - `Task<bool> SignInWithAppleAsync()`
   - `Task SignOutAsync()`
   - `Task<bool> ResetPasswordAsync(string email)`
   - `Task<UserProfile?> GetProfileAsync()`
   - `Task<bool> UpdateProfileAsync(UserProfile profile)`
   - `Task<string?> UploadAvatarAsync(string localFilePath)`
   - `bool IsLoggedIn { get; }`
   - `string? CurrentUserEmail { get; }`
   - `string? CurrentUserAvatarUrl { get; }`
   - `event EventHandler<bool>? AuthStateChanged`
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Supabase NuGet installed, IAuthService interface defines all auth operations, UserProfile model matches profiles table schema, SupabaseConfig has placeholder constants</done>
</task>

<task type="auto">
  <name>Task 2: Session persistence handler + platform OAuth configs</name>
  <files>
    AclsTracker/Services/Auth/SupabaseSessionHandler.cs,
    AclsTracker/Platforms/Android/WebAuthenticationCallbackActivity.cs,
    AclsTracker/Platforms/Android/AndroidManifest.xml,
    AclsTracker/Platforms/iOS/Info.plist,
    AclsTracker/Platforms/iOS/Entitlements.plist
  </files>
  <action>
1. Create `AclsTracker/Services/Auth/SupabaseSessionHandler.cs`:
   - Implements `IGotrueSessionPersistence<Session>` from `Supabase.Gotrue.Interfaces`
   - Uses `Supabase.Gotrue.Session` (not AclsTracker.Models.Session — be explicit with namespace)
   - `SaveSession`: Serialize session to JSON, store via `SecureStorage.Default.SetAsync` (use `MainThread.BeginInvokeOnMainThread` for async fire-and-forget)
   - `LoadSession`: Use `SecureStorage.Default.GetAsync(key).GetAwaiter().GetResult()` (interface is sync)
   - `DestroySession`: `SecureStorage.Default.Remove(key)`
   - Session key constant: `"supabase_session"`

2. Create `AclsTracker/Platforms/Android/WebAuthenticationCallbackActivity.cs`:
   - Activity with `[Activity(NoHistory = true, LaunchMode = LaunchMode.SingleTop, Exported = true)]`
   - IntentFilter: `ActionView`, categories `Default` + `Browsable`, `DataScheme = "aclstracker"`
   - Extends `Microsoft.Maui.Authentication.WebAuthenticatorCallbackActivity`

3. Modify `AclsTracker/Platforms/Android/AndroidManifest.xml`:
   - Add `<queries>` block with `<intent>` for `android.support.customtabs.action.CustomTabsService`
   - Place inside `<manifest>` element, outside `<application>`

4. Create or modify `AclsTracker/Platforms/iOS/Info.plist`:
   - Add `CFBundleURLTypes` array with `CFBundleURLSchemes` containing `"aclstracker"`
   - Set `CFBundleURLName` to `"ACLS Tracker"` and `CFBundleTypeRole` to `"Editor"`

5. Create or modify `AclsTracker/Platforms/iOS/Entitlements.plist`:
   - Add `com.apple.developer.applesignin` capability with value `["Default"]`
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>SupabaseSessionHandler compiles and implements IGotrueSessionPersistence, Android callback activity exists with aclstracker scheme, AndroidManifest has CustomTabs query, iOS plist has URL scheme, iOS entitlements has Apple Sign-In capability</done>
</task>

</tasks>

<verification>
- `dotnet build AclsTracker/AclsTracker.csproj` succeeds
- `IAuthService.cs` exists with all method signatures
- `SupabaseSessionHandler.cs` implements `IGotrueSessionPersistence<Session>`
- `UserProfile.cs` has all profile fields
- Android `WebAuthenticationCallbackActivity.cs` exists
- iOS `Info.plist` contains `aclstracker` URL scheme
</verification>

<success_criteria>
Project builds with Supabase NuGet, all contracts defined, platform OAuth callbacks configured for both Android and iOS. No runtime behavior yet — pure infrastructure.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-autenticacion-opcional-google-auth-apple-sign-in-email-password-con-verificacion/05.1-01-SUMMARY.md`
</output>
