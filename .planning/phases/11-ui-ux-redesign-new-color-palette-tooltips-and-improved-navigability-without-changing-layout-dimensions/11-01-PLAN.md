---
phase: 11-ui-ux-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AclsTracker/Resources/Styles/Colors.xaml
  - AclsTracker/Controls/TimerCard.xaml
  - AclsTracker/Controls/MetronomePulse.xaml
  - AclsTracker/Controls/MetronomePulse.xaml.cs
  - AclsTracker/Controls/RhythmSelector.xaml
  - AclsTracker/Controls/NotificationBanner.xaml
  - AclsTracker/Controls/EventLogPanel.xaml
  - AclsTracker/Views/MainPage.xaml
autonomous: true
requirements:
  - UX-11-PALETTE
  - UX-11-COLORS

must_haves:
  truths:
    - "All visual colors in the app come from named semantic resources in Colors.xaml, not inline hex"
    - "The clinical palette renders as clean whites, muted blues/grays in light mode"
    - "Dark mode colors are defined and applied via AppThemeBinding"
    - "Semantic rhythm colors preserved: red=TV/FV, amber=AESP/Asistolia, green=RCE"
    - "Active rhythm border uses dark navy instead of plain black"
  artifacts:
    - path: "AclsTracker/Resources/Styles/Colors.xaml"
      provides: "Central semantic color palette for entire app"
      contains: "SurfaceBackground"
    - path: "AclsTracker/Controls/TimerCard.xaml"
      provides: "Timer cards using resource colors"
      contains: "StaticResource"
    - path: "AclsTracker/Controls/RhythmSelector.xaml"
      provides: "Rhythm buttons using resource colors"
      contains: "StaticResource RhythmShockable"
  key_links:
    - from: "AclsTracker/Resources/Styles/Colors.xaml"
      to: "All XAML files"
      via: "StaticResource and AppThemeBinding references"
      pattern: "StaticResource (Surface|Action|Rhythm|Metronome|Timer|Banner|Content)"
    - from: "AclsTracker/Controls/MetronomePulse.xaml.cs"
      to: "Colors.xaml values"
      via: "C# Color.FromArgb constants matching resource values"
      pattern: "Color\\.FromArgb"
---

<objective>
Centralize the entire app color palette into semantic named resources in Colors.xaml and replace all hardcoded inline hex colors across all controls and views with StaticResource/AppThemeBinding references.

Purpose: Establish a single source of truth for the clinical/professional color palette per user decision. This enables consistent theming and is a prerequisite for the tooltip system (Plan 03) which needs the color tokens.

Output: Updated Colors.xaml with ~30 semantic color resources, all 6 controls and MainPage updated to reference them.
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-CONTEXT.md
@.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-RESEARCH.md

<interfaces>
<!-- Current Colors.xaml has basic palette (Primary, Gray100-950, Yellow/Cyan accents).
     Must be extended with semantic tokens. Existing keys must NOT be removed (Styles.xaml references them). -->

From AclsTracker/Resources/Styles/Colors.xaml:
- Primary (#D32F2F), PrimaryDark (#B71C1C), PrimaryLight (#EF5350)
- Secondary (#1565C0), SecondaryDark (#0D47A1), Tertiary (#2E7D32)
- Gray100-Gray950, White, Black
- Yellow100-300Accent, Cyan100-300Accent

From AclsTracker/App.xaml:
- MergedDictionaries: Colors.xaml + Styles.xaml
- Global converters: BoolToColorConverter, IsNotNullConverter, InvertBoolConverter, PauseIconConverter, BoolToActiveColorConverter, BoolToActiveTextColorConverter, BoolToOnOffConverter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define semantic color palette in Colors.xaml</name>
  <files>AclsTracker/Resources/Styles/Colors.xaml</files>
  <action>
Extend Colors.xaml (do NOT remove any existing keys — Styles.xaml depends on them). Add the following semantic color groups AFTER the existing definitions:

**Clinical base palette (light mode):**
- `SurfaceBackground` = #FFFFFF (page backgrounds)
- `SurfaceCard` = #F4F6F9 (card/frame backgrounds — slightly blue-tinged gray, more clinical than warm #F5F5F5)
- `SurfaceBorder` = #DDE2EA (card borders)
- `SurfaceBackgroundDark` = #111318 (dark page bg)
- `SurfaceCardDark` = #1C2130 (dark card bg)
- `SurfaceBorderDark` = #2E3547 (dark card border)

**Content text colors:**
- `ContentPrimary` = #1A2332 (main text — dark navy, not pure black)
- `ContentSecondary` = #5A6478 (secondary text)
- `ContentMuted` = #9AA0AE (muted/subtle text — replaces #999999)
- `ContentPrimaryDark` = #E8ECF1 (dark mode main text)
- `ContentSecondaryDark` = #9AA0AE (dark mode secondary)

**Clinical accent:**
- `AccentClinical` = #1A73B8 (primary accent blue)
- `AccentClinicalDark` = #2D8FD4 (dark mode accent)

**Rhythm semantic (preserved meanings, refined tones):**
- `RhythmShockable` = #C62828 (TV/FV — slightly deeper red)
- `RhythmNonShockable` = #F9A825 (AESP/Asistolia — refined amber)
- `RhythmNonShockableText` = #212121 (dark text on amber)
- `RhythmRCE` = #2E7D32 (RCE green — unchanged)
- `RhythmSelectedBorder` = #1A2332 (active rhythm — dark navy instead of black)

**Action buttons:**
- `ActionStartCode` = #D84315 (INICIAR CODIGO — deep orange)
- `ActionStopCode` = #546E7A (FINALIZAR — blue-gray, more clinical than #757575)
- `ActionNewCycle` = #E65100 (NUEVO CICLO)
- `ActionDefibrillate` = #B71C1C (DEFIBRILAR)
- `ActionDrug` = #6A1B9A (ADRENALINA/AMIODARONA)
- `ActionDrugSuggested` = #C62828 (drug when suggested)

**Metronome:**
- `MetronomeRest` = #C62828 (circle rest color)
- `MetronomeFlash` = #FF6659 (circle flash color)
- `MetronomeToggle` = #1565C0 (ON/OFF button light)
- `MetronomeToggleDark` = #1976D2 (ON/OFF button dark)
- `MetronomeBpmButton` = #E0E0E0 (light +/- bg)
- `MetronomeBpmButtonDark` = #333333 (dark +/- bg)

**Timer state:**
- `TimerOverThresholdBg` = #FFEBEE
- `TimerOverThresholdText` = #C62828
- `TimerExtraInfo` = #9AA0AE (matches ContentMuted)
- `TimerPauseIcon` = #FF6F00

**Banner:**
- `BannerBackground` = #FFF8E1 (warmer, more clinical than #FFF3C2)
- `BannerText` = #37474F (blue-gray text, more clinical than #333333)

**Tooltip (for Plan 03):**
- `TooltipBackground` = #1A2332
- `TooltipText` = #FFFFFF
- `TooltipIcon` = #5A9FD4
  </action>
  <verify>
    <automated>cd "C:/Users/Adria/Documents/code/CPRHelper" && grep -c "x:Key=" AclsTracker/Resources/Styles/Colors.xaml</automated>
  </verify>
  <done>Colors.xaml contains 40+ named semantic color resources. All existing keys preserved. New keys follow naming convention: Category + Role (e.g., SurfaceCard, ActionDrug, RhythmShockable).</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline hex colors with StaticResource references across all controls and MainPage</name>
  <files>
    AclsTracker/Controls/TimerCard.xaml,
    AclsTracker/Controls/MetronomePulse.xaml,
    AclsTracker/Controls/MetronomePulse.xaml.cs,
    AclsTracker/Controls/RhythmSelector.xaml,
    AclsTracker/Controls/NotificationBanner.xaml,
    AclsTracker/Controls/EventLogPanel.xaml,
    AclsTracker/Views/MainPage.xaml
  </files>
  <action>
Replace every hardcoded inline hex color in these files with the corresponding semantic StaticResource. Use `{AppThemeBinding Light={StaticResource X}, Dark={StaticResource XDark}}` where dark variants exist; use `{StaticResource X}` where the color is the same in both themes.

**MainPage.xaml:**
- Page `BackgroundColor` → `{AppThemeBinding Light={StaticResource SurfaceBackground}, Dark={StaticResource SurfaceBackgroundDark}}`
- INICIAR CODIGO `BackgroundColor="#E65100"` → `{StaticResource ActionStartCode}`
- FINALIZAR CODIGO `BackgroundColor="#757575"` → `{StaticResource ActionStopCode}`
- NUEVO CICLO `BackgroundColor="#FF9800"` → `{StaticResource ActionNewCycle}`
- DEFIBRILAR `BackgroundColor="#C62828"` → `{StaticResource ActionDefibrillate}`
- ADRENALINA/AMIODARONA `BackgroundColor="#7B1FA2"` → `{StaticResource ActionDrug}`
- DataTrigger drug suggested `Value="#D32F2F"` → `{StaticResource ActionDrugSuggested}`

**TimerCard.xaml:**
- Frame `BackgroundColor` → `{AppThemeBinding Light={StaticResource SurfaceCard}, Dark={StaticResource SurfaceCardDark}}`
- Frame `BorderColor` → `{AppThemeBinding Light={StaticResource SurfaceBorder}, Dark={StaticResource SurfaceBorderDark}}`
- Timer name `TextColor` → `{AppThemeBinding Light={StaticResource ContentPrimary}, Dark={StaticResource ContentSecondaryDark}}`
- Elapsed `TextColor` → `{AppThemeBinding Light={StaticResource ContentPrimary}, Dark={StaticResource ContentPrimaryDark}}`
- DataTrigger `IsOverThreshold` Frame bg → `{StaticResource TimerOverThresholdBg}`
- DataTrigger `IsOverThreshold` Label color → `{StaticResource TimerOverThresholdText}`
- ExtraInfo `TextColor="#999999"` → `{StaticResource TimerExtraInfo}`
- Pause button `TextColor="#FF6F00"` → `{StaticResource TimerPauseIcon}`

**MetronomePulse.xaml:**
- PulseCircle `Color="#D32F2F"` → `{StaticResource MetronomeRest}`
- BPM label `TextColor` → `{AppThemeBinding Light={StaticResource ContentPrimary}, Dark={StaticResource ContentPrimaryDark}}`
- "BPM" subtitle → `{AppThemeBinding Light={StaticResource ContentSecondary}, Dark={StaticResource ContentSecondaryDark}}`
- +/- buttons `BackgroundColor` → `{AppThemeBinding Light={StaticResource MetronomeBpmButton}, Dark={StaticResource MetronomeBpmButtonDark}}`
- +/- buttons `TextColor` → `{AppThemeBinding Light={StaticResource ContentPrimary}, Dark={StaticResource ContentPrimaryDark}}`
- Toggle button `BackgroundColor` → `{AppThemeBinding Light={StaticResource MetronomeToggle}, Dark={StaticResource MetronomeToggleDark}}`

**MetronomePulse.xaml.cs:**
- Update `RestColor = Color.FromArgb("#C62828")` to match `MetronomeRest` value
- Update `FlashColor = Color.FromArgb("#FF6659")` to match `MetronomeFlash` value
- NOTE: These remain as C# constants because the animation requires programmatic color interpolation. The values must MATCH what is in Colors.xaml but cannot use StaticResource from C# code-behind in this pattern.

**RhythmSelector.xaml:**
- RCE `BackgroundColor="#2E7D32"` → `{StaticResource RhythmRCE}`
- AESP `BackgroundColor="#FBC02D"` → `{StaticResource RhythmNonShockable}`
- AESP `TextColor="#333333"` → `{StaticResource RhythmNonShockableText}`
- ASISTOLIA same as AESP
- TV `BackgroundColor="#D32F2F"` → `{StaticResource RhythmShockable}`
- FV same as TV
- All selected DataTrigger `BorderColor="Black"` → `{StaticResource RhythmSelectedBorder}`
- All selected DataTrigger `BorderWidth="3"` → keep at 3 (or change to 2.5 for slightly more refined look)

**NotificationBanner.xaml:**
- Grid `BackgroundColor="#FFF3C2"` → `{StaticResource BannerBackground}`
- Label `TextColor="#333333"` → `{StaticResource BannerText}`

**EventLogPanel.xaml:**
- Header `TextColor="#999999"` → `{StaticResource ContentMuted}`
- Timestamp `TextColor="#999999"` → `{StaticResource ContentMuted}`
- DataTrigger RhythmChange `Value="#D32F2F"` → `{StaticResource RhythmShockable}`
- DataTrigger Medication `Value="#7B1FA2"` → `{StaticResource ActionDrug}`
- DataTrigger CprCycle `Value="#FF9800"` → `{StaticResource ActionNewCycle}`
- DataTrigger HsTs `Value="#1565C0"` → `{StaticResource AccentClinical}`
- DataTrigger Custom `Value="#333333"` → `{StaticResource ContentPrimary}`

IMPORTANT: If any `<Setter Value="{StaticResource X}">` inside a DataTrigger does not compile (known MAUI issue), leave that specific Setter with the hex value as a comment noting the resource key it maps to.
  </action>
  <verify>
    <automated>cd "C:/Users/Adria/Documents/code/CPRHelper" && dotnet build AclsTracker/AclsTracker.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Zero hardcoded hex colors remain in the 7 listed XAML files (except possibly DataTrigger Setter fallbacks documented with comments). Build succeeds. All colors flow from Colors.xaml semantic resources.</done>
</task>

</tasks>

<verification>
- `dotnet build` passes with no errors
- `grep -rn '#[0-9A-Fa-f]\{6\}' AclsTracker/Controls/*.xaml AclsTracker/Views/MainPage.xaml` returns zero matches (or only commented fallbacks)
- Colors.xaml has 40+ `x:Key` entries
</verification>

<success_criteria>
- All inline hex colors replaced with named semantic resources from Colors.xaml
- Clinical palette renders clean whites, muted blues/grays (visual verification on device)
- Dark mode variants defined for all surface and content colors
- Rhythm semantic colors preserved (red/amber/green meaning unchanged)
- Build compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-01-SUMMARY.md`
</output>
