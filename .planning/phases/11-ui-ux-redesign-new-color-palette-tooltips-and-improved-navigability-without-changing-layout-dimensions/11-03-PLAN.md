---
phase: 11-ui-ux-redesign
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - AclsTracker/Controls/TooltipOverlay.xaml
  - AclsTracker/Controls/TooltipOverlay.xaml.cs
  - AclsTracker/Views/MainPage.xaml
  - AclsTracker/Views/MainPage.xaml.cs
  - AclsTracker/Controls/TimerCard.xaml
  - AclsTracker/Controls/MetronomePulse.xaml
  - AclsTracker/Controls/RhythmSelector.xaml
autonomous: false
requirements:
  - UX-11-TOOLTIPS

must_haves:
  truths:
    - "User can tap an info icon next to key elements and see a tooltip with protocol/usage info"
    - "Tooltips auto-dismiss after 3-4 seconds"
    - "User can tap backdrop to dismiss tooltip earlier"
    - "Tooltips work during active codes without disrupting timers or metronome"
    - "Tooltip content combines protocol information and usage guidance"
  artifacts:
    - path: "AclsTracker/Controls/TooltipOverlay.xaml"
      provides: "Reusable tooltip overlay ContentView"
      contains: "TooltipLabel"
    - path: "AclsTracker/Controls/TooltipOverlay.xaml.cs"
      provides: "Show/Hide logic with IDispatcherTimer auto-dismiss"
      contains: "IDispatcherTimer"
      exports: ["Show", "Hide"]
    - path: "AclsTracker/Views/MainPage.xaml"
      provides: "TooltipOverlay instance and info icon labels"
      contains: "TooltipOverlay"
  key_links:
    - from: "AclsTracker/Views/MainPage.xaml"
      to: "AclsTracker/Controls/TooltipOverlay.xaml"
      via: "controls:TooltipOverlay x:Name in Grid overlay"
      pattern: "TooltipOverlay"
    - from: "AclsTracker/Views/MainPage.xaml.cs"
      to: "TooltipOverlay.Show()"
      via: "TapGestureRecognizer event handlers calling Show(text)"
      pattern: "TooltipControl\\.Show"
---

<objective>
Create a TooltipOverlay control and add info icons (with protocol/usage tooltips) to key elements on MainPage, timer cards, metronome, and rhythm selector.

Purpose: Help both new users and experienced clinicians quickly access protocol details (FCT%, dosing rules, rhythm categories) without leaving the active code screen. Tooltips are always available per user decision.

Output: New TooltipOverlay control, info icons on 6-8 high-priority elements, auto-dismiss behavior.
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-CONTEXT.md
@.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-RESEARCH.md
@.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-01-SUMMARY.md
@.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 Colors.xaml (will exist when this plan runs): -->
TooltipBackground = #1A2332
TooltipText = #FFFFFF
TooltipIcon = #5A9FD4

<!-- NotificationBanner pattern (model for TooltipOverlay): -->
From AclsTracker/Controls/NotificationBanner.xaml.cs:
```csharp
public partial class NotificationBanner : ContentView, INotifyPropertyChanged
{
    private IDispatcherTimer? _dismissTimer;
    public void Show(string message, int autoDismissSeconds = 5) { ... }
    public async void HideBanner() { ... }
}
```

<!-- MainPage.xaml after Plan 01+02 will have:
     - Root Grid with ScrollView + NotificationBanner (ZIndex=10) + TabBarToggle (ZIndex=5)
     - No EventLogPanel
     - All colors using StaticResource references
     - OnToggleTabBarTapped handler in code-behind -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TooltipOverlay control and wire into MainPage with info icons</name>
  <files>
    AclsTracker/Controls/TooltipOverlay.xaml,
    AclsTracker/Controls/TooltipOverlay.xaml.cs,
    AclsTracker/Views/MainPage.xaml,
    AclsTracker/Views/MainPage.xaml.cs
  </files>
  <action>
**Create AclsTracker/Controls/TooltipOverlay.xaml:**

A ContentView overlay modeled on NotificationBanner. Structure:
```xml
&lt;?xml version="1.0" encoding="utf-8" ?&gt;
&lt;ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AclsTracker.Controls.TooltipOverlay"
             IsVisible="False"
             InputTransparent="False"&gt;
    &lt;Grid&gt;
        &lt;!-- Semi-transparent backdrop — tap to dismiss --&gt;
        &lt;BoxView BackgroundColor="#40000000" InputTransparent="False"&gt;
            &lt;BoxView.GestureRecognizers&gt;
                &lt;TapGestureRecognizer Tapped="OnBackdropTapped" /&gt;
            &lt;/BoxView.GestureRecognizers&gt;
        &lt;/BoxView&gt;
        &lt;!-- Tooltip card --&gt;
        &lt;Frame BackgroundColor="{StaticResource TooltipBackground}"
               CornerRadius="12"
               Padding="16,12"
               HasShadow="True"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               Margin="24"
               MaximumWidthRequest="300"&gt;
            &lt;Label x:Name="TooltipLabel"
                   TextColor="{StaticResource TooltipText}"
                   FontSize="14"
                   LineBreakMode="WordWrap" /&gt;
        &lt;/Frame&gt;
    &lt;/Grid&gt;
&lt;/ContentView&gt;
```

**Create AclsTracker/Controls/TooltipOverlay.xaml.cs:**

```csharp
namespace AclsTracker.Controls;

public partial class TooltipOverlay : ContentView
{
    private IDispatcherTimer? _dismissTimer;

    public TooltipOverlay()
    {
        InitializeComponent();
    }

    public void Show(string tooltipText, int autoDismissSeconds = 4)
    {
        TooltipLabel.Text = tooltipText;
        this.Opacity = 0;
        IsVisible = true;
        this.FadeTo(1.0, 200);

        _dismissTimer?.Stop();
        _dismissTimer = Application.Current!.Dispatcher.CreateTimer();
        _dismissTimer.Interval = TimeSpan.FromSeconds(autoDismissSeconds);
        _dismissTimer.IsRepeating = false;
        _dismissTimer.Tick += (_, _) =>
        {
            _dismissTimer.Stop();
            Hide();
        };
        _dismissTimer.Start();
    }

    public async void Hide()
    {
        _dismissTimer?.Stop();
        await this.FadeTo(0, 150);
        MainThread.BeginInvokeOnMainThread(() => IsVisible = false);
    }

    private void OnBackdropTapped(object sender, EventArgs e)
    {
        Hide();
    }
}
```

**MainPage.xaml changes:**

Add the TooltipOverlay as the highest ZIndex overlay in the root Grid (after NotificationBanner):
```xml
&lt;controls:TooltipOverlay x:Name="TooltipControl"
                          VerticalOptions="Fill"
                          HorizontalOptions="Fill"
                          ZIndex="20" /&gt;
```

Add info icon labels next to HIGH-priority elements. Each icon is a small Label with TapGestureRecognizer:

1. **Near the timer grid** (above Block 4) — Add a small info row:
   ```xml
   &lt;HorizontalStackLayout HorizontalOptions="End" Padding="0,0,4,0"&gt;
       &lt;Label Text="i" FontSize="13" TextColor="{StaticResource TooltipIcon}" VerticalOptions="Center"&gt;
           &lt;Label.GestureRecognizers&gt;
               &lt;TapGestureRecognizer Tapped="OnTimerInfoTapped" /&gt;
           &lt;/Label.GestureRecognizers&gt;
       &lt;/Label&gt;
   &lt;/HorizontalStackLayout&gt;
   ```

2. **Above the action buttons grid** (above Block 5):
   ```xml
   &lt;HorizontalStackLayout HorizontalOptions="End" Padding="0,0,4,0"&gt;
       &lt;Label Text="i Acciones" FontSize="11" TextColor="{StaticResource TooltipIcon}" VerticalOptions="Center"&gt;
           &lt;Label.GestureRecognizers&gt;
               &lt;TapGestureRecognizer Tapped="OnActionsInfoTapped" /&gt;
           &lt;/Label.GestureRecognizers&gt;
       &lt;/Label&gt;
   &lt;/HorizontalStackLayout&gt;
   ```

3. **Near the rhythm selector** (above Block 3):
   ```xml
   &lt;HorizontalStackLayout HorizontalOptions="End" Padding="0,0,4,0"&gt;
       &lt;Label Text="i" FontSize="13" TextColor="{StaticResource TooltipIcon}" VerticalOptions="Center"&gt;
           &lt;Label.GestureRecognizers&gt;
               &lt;TapGestureRecognizer Tapped="OnRhythmInfoTapped" /&gt;
           &lt;/Label.GestureRecognizers&gt;
       &lt;/Label&gt;
   &lt;/HorizontalStackLayout&gt;
   ```

NOTE: Use the Unicode character U+24D8 (circled latin small letter i) or the plain "i" with a circle border for the info icon. The exact glyph choice is discretionary.

**MainPage.xaml.cs — Add tooltip event handlers:**

```csharp
private void OnTimerInfoTapped(object sender, EventArgs e)
{
    TooltipControl.Show(
        "Timers: T. Total = duracion del codigo. Ciclo RCP = 2 min por ciclo. " +
        "T. Comp = tiempo en compresiones (FCT% = fraccion del tiempo total). " +
        "Adrenalina: cada 3-5 min. Amiodarona: max 3 dosis. " +
        "T. Pulso = tiempo sin pulso (rojo > 10s).", 4);
}

private void OnActionsInfoTapped(object sender, EventArgs e)
{
    TooltipControl.Show(
        "NUEVO CICLO: Inicia ciclo de 2 min de RCP. " +
        "DEFIBRILAR: Solo para TV/FV — 200J bifasico, CPR inmediata post-descarga. " +
        "ADRENALINA: 1mg IV/IO cada 3-5 min. " +
        "AMIODARONA: 300mg 1ra dosis, 150mg 2da y 3ra.", 4);
}

private void OnRhythmInfoTapped(object sender, EventArgs e)
{
    TooltipControl.Show(
        "Ritmos cardiacos: RCE (verde) = pulso palpable, manejo post-RCE. " +
        "AESP/Asistolia (amarillo) = no desfibrilable, buscar H's y T's. " +
        "TV/FV (rojo) = desfibrilable, preparar descarga.", 4);
}
```

CRITICAL: Do NOT use CommunityToolkit.Maui Popup for tooltips — it triggers page lifecycle events that would disrupt active timers (per research pitfall #2). The ContentView overlay approach keeps everything in the same page.
  </action>
  <verify>
    <automated>cd "C:/Users/Adria/Documents/code/CPRHelper" && dotnet build AclsTracker/AclsTracker.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>TooltipOverlay.xaml and .cs exist. MainPage includes TooltipOverlay at ZIndex=20. Three info icons (timers, actions, rhythm) are tappable and show protocol tooltips. Tooltips auto-dismiss after 4 seconds. Backdrop tap dismisses early. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 11 UI/UX redesign on device</name>
  <action>
Human verification of the complete Phase 11 UI/UX redesign across all 3 plans:
- Plan 01: Clinical color palette centralized in Colors.xaml, all inline hex replaced
- Plan 02: EventLogPanel removed from MainPage, collapsible tab bar with toggle button
- Plan 03: Tooltip system with info icons and auto-dismiss overlays

Steps to verify:
1. Deploy to Android emulator: `dotnet build -t:Run -f net8.0-android`
2. Color palette check: Verify MainPage has clean, clinical appearance (muted blues/grays, not harsh blacks). Verify rhythm buttons: RCE=green, AESP/Asistolia=amber, TV/FV=red. Verify active rhythm selection has dark navy border (not black). Verify INICIAR CODIGO is deep orange, action buttons have distinct colors. Switch to dark mode in Android settings, verify all colors adapt.
3. EventLogPanel removed: Scroll MainPage fully — no event log section at bottom. Start a code, select rhythms, press buttons — verify events still recorded (check Historial tab).
4. Tab bar toggle: On MainPage, tab bar should be HIDDEN by default. Tap the floating button (bottom-right) — tab bar appears. Tap again — tab bar hides. When visible, navigate to Causas Reversibles and Historial — verify all work.
5. Tooltips: Tap info icon near timers — tooltip appears with timer descriptions, auto-dismisses after ~4 seconds. Tap info near rhythm selector — see rhythm categories explanation. Tap info near action buttons — see protocol info for each button. Tap backdrop while tooltip is showing — it dismisses immediately. Start a code with metronome ON, show a tooltip — verify metronome continues without interruption.
  </action>
  <verify>User confirms visual appearance and functionality on device</verify>
  <done>User approves the complete Phase 11 UI/UX redesign or describes issues to fix</done>
</task>

</tasks>

<verification>
- `dotnet build` passes
- TooltipOverlay.xaml and .cs exist in Controls/
- MainPage.xaml contains `TooltipOverlay` reference
- MainPage.xaml.cs contains `TooltipControl.Show` calls
- No CommunityToolkit Popup used for tooltips
</verification>

<success_criteria>
- Info icons visible next to timer grid, action buttons, and rhythm selector
- Tapping any info icon shows a tooltip with protocol + usage content
- Tooltips auto-dismiss after ~4 seconds
- Tapping backdrop dismisses tooltip immediately
- Metronome and timers continue uninterrupted during tooltip display
- User confirms visual appearance on device
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-ux-redesign-new-color-palette-tooltips-and-improved-navigability-without-changing-layout-dimensions/11-03-SUMMARY.md`
</output>
