---
phase: 09-fix-ui-issues-and-authentication
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - AclsTracker/Views/MainPage.xaml
  - AclsTracker/Controls/RhythmSelector.xaml
autonomous: true
requirements:
  - P09-UI-03
  - P09-UI-04

must_haves:
  truths:
    - "INICIAR CODIGO button is orange (#E65100) with height 40 (not red, not 48)"
    - "FINALIZAR CODIGO button stays gray (#757575) with height 40 (not 48)"
    - "AESP button is yellow (#FBC02D) with dark text (#333333)"
    - "ASISTOLIA button is yellow (#FBC02D) with dark text (#333333)"
  artifacts:
    - path: "AclsTracker/Views/MainPage.xaml"
      provides: "INICIAR/FINALIZAR button color and height"
      contains: "#E65100"
    - path: "AclsTracker/Controls/RhythmSelector.xaml"
      provides: "AESP/ASISTOLIA button colors"
      contains: "#FBC02D"
  key_links:
    - from: "MainPage.xaml INICIAR button"
      to: "User visual feedback"
      via: "BackgroundColor #E65100 matches pre-change AESP orange"
      pattern: "BackgroundColor.*#E65100"
    - from: "RhythmSelector.xaml AESP button"
      to: "RhythmSelector.xaml ASISTOLIA button"
      via: "Same yellow color + dark text for non-shockable rhythm grouping"
      pattern: "BackgroundColor.*#FBC02D"
---

<objective>
Fix button colors and sizes: change INICIAR CODIGO to orange (#E65100), reduce both code buttons height from 48 to 40, and change non-defibrillable rhythm buttons (AESP + ASISTOLIA) from orange to yellow with dark text for accessibility.

Purpose: Visual differentiation — code buttons should use the captured AESP orange (before AESP changes to yellow in the same phase), and non-shockable rhythms should visually distinguish from shockable ones (TV/FV stay red).

Output: Corrected button colors and sizing across MainPage and RhythmSelector.
</objective>

<execution_context>
@C:/Users/Adria/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fix-ui-issues-and-authentication-google-auth-redirect-loop-persistent-login-button-sizing-and-colors-on-off-toggle-metronome-start-end-code-button-non-defib-rhythms-color/09-CONTEXT.md
@.planning/phases/09-fix-ui-issues-and-authentication-google-auth-redirect-loop-persistent-login-button-sizing-and-colors-on-off-toggle-metronome-start-end-code-button-non-defib-rhythms-color/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update INICIAR/FINALIZAR CODIGO button colors and height</name>
  <files>
    AclsTracker/Views/MainPage.xaml
  </files>
  <action>
Edit `AclsTracker/Views/MainPage.xaml` — two button changes (lines 28–45):

**Change 1: INICIAR CODIGO button (lines 28–36)**

Change BackgroundColor from `#D32F2F` (red) to `#E65100` (orange — the current AESP orange, captured before AESP changes to yellow).
Change HeightRequest from `48` to `40`.

```xml
<!-- BEFORE -->
<Button Text="INICIAR CODIGO"
        Command="{Binding StartCodeCommand}"
        BackgroundColor="#D32F2F"
        TextColor="White"
        FontSize="16" FontAttributes="Bold"
        HeightRequest="48"
        CornerRadius="8"
        HorizontalOptions="Fill"
        IsVisible="{Binding Timer.IsSessionActive, Converter={StaticResource InvertBoolConverter}}" />

<!-- AFTER -->
<Button Text="INICIAR CODIGO"
        Command="{Binding StartCodeCommand}"
        BackgroundColor="#E65100"
        TextColor="White"
        FontSize="16" FontAttributes="Bold"
        HeightRequest="40"
        CornerRadius="8"
        HorizontalOptions="Fill"
        IsVisible="{Binding Timer.IsSessionActive, Converter={StaticResource InvertBoolConverter}}" />
```

**Change 2: FINALIZAR CODIGO button (lines 37–45)**

Keep BackgroundColor as `#757575` (gray — unchanged).
Change HeightRequest from `48` to `40`.

```xml
<!-- BEFORE -->
<Button Text="FINALIZAR CODIGO"
        Command="{Binding StopCodeCommand}"
        BackgroundColor="#757575"
        TextColor="White"
        FontSize="16" FontAttributes="Bold"
        HeightRequest="48"
        CornerRadius="8"
        HorizontalOptions="Fill"
        IsVisible="{Binding Timer.IsSessionActive}" />

<!-- AFTER -->
<Button Text="FINALIZAR CODIGO"
        Command="{Binding StopCodeCommand}"
        BackgroundColor="#757575"
        TextColor="White"
        FontSize="16" FontAttributes="Bold"
        HeightRequest="40"
        CornerRadius="8"
        HorizontalOptions="Fill"
        IsVisible="{Binding Timer.IsSessionActive}" />
```

**DO NOT change:** Text, Command, FontSize, FontAttributes, CornerRadius, HorizontalOptions, IsVisible for either button.
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj -c Debug -f net8.0-android</automated>
  </verify>
  <done>
    - INICIAR CODIGO button has BackgroundColor="#E65100" and HeightRequest="40"
    - FINALIZAR CODIGO button has BackgroundColor="#757575" (unchanged) and HeightRequest="40"
    - All other button properties unchanged
    - Project builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AESP + ASISTOLIA rhythm button colors</name>
  <files>
    AclsTracker/Controls/RhythmSelector.xaml
  </files>
  <action>
Edit `AclsTracker/Controls/RhythmSelector.xaml` — change two buttons from orange to yellow with dark text.

**Change 1: AESP button (lines 38–55)**

Change BackgroundColor from `#E65100` to `#FBC02D` (ámber fuerte).
Change TextColor from `White` to `#333333` (dark — WCAG AA contrast ~10:1 on yellow).

```xml
<!-- BEFORE (lines 38-55) -->
<Button Grid.Column="1"
        Text="AESP"
        Command="{Binding SelectRhythmCommand}"
        CommandParameter="{x:Static models:CardiacRhythm.AESP}"
        BackgroundColor="#E65100"
        TextColor="White" FontSize="11" FontAttributes="Bold"
        HeightRequest="36" CornerRadius="8" Padding="2,0"
        BorderWidth="0" BorderColor="Transparent">
    <Button.Triggers>
        <DataTrigger TargetType="Button"
                     Binding="{Binding IsRhythmAESP}"
                     Value="True">
            <Setter Property="BorderColor" Value="White" />
            <Setter Property="BorderWidth" Value="2" />
        </DataTrigger>
    </Button.Triggers>
</Button>

<!-- AFTER -->
<Button Grid.Column="1"
        Text="AESP"
        Command="{Binding SelectRhythmCommand}"
        CommandParameter="{x:Static models:CardiacRhythm.AESP}"
        BackgroundColor="#FBC02D"
        TextColor="#333333" FontSize="11" FontAttributes="Bold"
        HeightRequest="36" CornerRadius="8" Padding="2,0"
        BorderWidth="0" BorderColor="Transparent">
    <Button.Triggers>
        <DataTrigger TargetType="Button"
                     Binding="{Binding IsRhythmAESP}"
                     Value="True">
            <Setter Property="BorderColor" Value="White" />
            <Setter Property="BorderWidth" Value="2" />
        </DataTrigger>
    </Button.Triggers>
</Button>
```

**Change 2: ASISTOLIA button (lines 57–74)**

Same changes — BackgroundColor `#E65100` → `#FBC02D`, TextColor `White` → `#333333`.

```xml
<!-- BEFORE (lines 57-74) -->
<Button Grid.Column="2"
        Text="ASISTOLIA"
        Command="{Binding SelectRhythmCommand}"
        CommandParameter="{x:Static models:CardiacRhythm.Asistolia}"
        BackgroundColor="#E65100"
        TextColor="White" FontSize="11" FontAttributes="Bold"
        HeightRequest="36" CornerRadius="8" Padding="2,0"
        BorderWidth="0" BorderColor="Transparent">
    <Button.Triggers>
        <DataTrigger TargetType="Button"
                     Binding="{Binding IsRhythmAsistolia}"
                     Value="True">
            <Setter Property="BorderColor" Value="White" />
            <Setter Property="BorderWidth" Value="2" />
        </DataTrigger>
    </Button.Triggers>
</Button>

<!-- AFTER -->
<Button Grid.Column="2"
        Text="ASISTOLIA"
        Command="{Binding SelectRhythmCommand}"
        CommandParameter="{x:Static models:CardiacRhythm.Asistolia}"
        BackgroundColor="#FBC02D"
        TextColor="#333333" FontSize="11" FontAttributes="Bold"
        HeightRequest="36" CornerRadius="8" Padding="2,0"
        BorderWidth="0" BorderColor="Transparent">
    <Button.Triggers>
        <DataTrigger TargetType="Button"
                     Binding="{Binding IsRhythmAsistolia}"
                     Value="True">
            <Setter Property="BorderColor" Value="White" />
            <Setter Property="BorderWidth" Value="2" />
        </DataTrigger>
    </Button.Triggers>
</Button>
```

**DO NOT change:** RCE (green), TV (red), FV (red) buttons — they stay as-is.
**DO NOT change:** DataTrigger (white border selection indicator) — white border still reads over yellow.
**DO NOT change:** HeightRequest, CornerRadius, Padding, FontSize, FontAttributes, Commands.
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj -c Debug -f net8.0-android</automated>
  </verify>
  <done>
    - AESP button has BackgroundColor="#FBC02D" and TextColor="#333333"
    - ASISTOLIA button has BackgroundColor="#FBC02D" and TextColor="#333333"
    - RCE, TV, FV buttons unchanged (green, red, red)
    - DataTrigger selection indicators (white border) preserved
    - Project builds without errors
  </done>
</task>

</tasks>

<verification>
1. `dotnet build AclsTracker/AclsTracker.csproj -c Debug -f net8.0-android` — compiles without errors
2. Visual check on emulator: INICIAR is orange, AESP/ASISTOLIA are yellow with dark text, RCE green/TV+ FV red unchanged
</verification>

<success_criteria>
- INICIAR CODIGO button is orange (#E65100) at height 40
- FINALIZAR CODIGO button stays gray at height 40
- AESP and ASISTOLIA buttons are yellow (#FBC02D) with dark text (#333333)
- Other rhythm buttons (RCE, TV, FV) unchanged
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-ui-issues-and-authentication-google-auth-redirect-loop-persistent-login-button-sizing-and-colors-on-off-toggle-metronome-start-end-code-button-non-defib-rhythms-color/09-03-SUMMARY.md`
</output>
