---
phase: 01-metronome-timers
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - AclsTracker/Views/MainPage.xaml
  - AclsTracker/Views/MainPage.xaml.cs
  - AclsTracker/Controls/MetronomePulse.xaml
  - AclsTracker/Controls/MetronomePulse.xaml.cs
  - AclsTracker/Controls/TimerCard.xaml
  - AclsTracker/Controls/TimerCard.xaml.cs
  - AclsTracker/ViewModels/MainViewModel.cs
  - AclsTracker/MauiProgram.cs
  - AclsTracker/AppShell.xaml
autonomous: false
requirements: [AUDI-01, AUDI-02, TIME-01, TIME-02, TIME-03]

must_haves:
  truths:
    - "User can start/stop audio metronome at configurable BPM (100-120) via large touch button"
    - "User sees pulsing circle animation synchronized with each metronome beat at 60fps"
    - "User sees all timers simultaneously on one screen with digital format (00:00) and progress circles"
    - "User can independently start, pause, and reset each timer via per-timer buttons"
    - "User sees elapsed time updating in real-time for all active timers"
  artifacts:
    - path: "AclsTracker/Views/MainPage.xaml"
      provides: "Single-screen layout with metronome + all timers (per D-04)"
      contains: "MetronomePulse"
    - path: "AclsTracker/Controls/MetronomePulse.xaml"
      provides: "Animated pulse circle for visual metronome (per D-03)"
      contains: "Ellipse"
    - path: "AclsTracker/Controls/TimerCard.xaml"
      provides: "Timer display with digital format and progress (per D-05)"
      contains: "ProgressBar"
    - path: "AclsTracker/ViewModels/MainViewModel.cs"
      provides: "Composite ViewModel combining metronome + timer ViewModels"
      contains: "class MainViewModel"
  key_links:
    - from: "AclsTracker/Views/MainPage.xaml"
      to: "AclsTracker/ViewModels/MainViewModel.cs"
      via: "BindingContext in code-behind constructor"
      pattern: "BindingContext"
    - from: "AclsTracker/Controls/MetronomePulse.xaml"
      to: "MetronomeViewModel.BeatPulse"
      via: "PropertyChanged trigger on BeatPulse for scale animation"
      pattern: "BeatPulse"
    - from: "AclsTracker/Controls/TimerCard.xaml"
      to: "TimerModel.Elapsed"
      via: "Binding to Elapsed with StringFormat for digital display"
      pattern: "Elapsed"
    - from: "AclsTracker/MauiProgram.cs"
      to: "MainPage and MainViewModel"
      via: "DI registration"
      pattern: "AddTransient.*MainPage"
---

<objective>
Build the single-screen UI that combines metronome visualization and all timers into one emergency-ready interface.

Purpose: Wire the metronome engine (Plan 02) and timer system (Plan 03) to a visual interface. AUDI-02 requires 60fps visual sync. D-03 specifies pulse animation. D-04 requires all timers on same screen. D-05 specifies digital format with progress circles. This completes Phase 1 by making all timing features user-accessible.
Output: Working MainPage with MetronomePulse control, TimerCard controls, and all UI bindings active.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-metronome-timers/01-Metronome-Timers-CONTEXT.md

Depends on Plan 02 and Plan 03 outputs:
- MetronomeViewModel from `AclsTracker/ViewModels/MetronomeViewModel.cs`
- TimerViewModel from `AclsTracker/ViewModels/TimerViewModel.cs`
- TimerModel from `AclsTracker/Models/TimerModel.cs`

<interfaces>
<!-- Key types the executor needs for UI binding. From Plans 01-03. -->

From AclsTracker/ViewModels/MetronomeViewModel.cs:
```csharp
public partial class MetronomeViewModel : ObservableObject
{
    [ObservableProperty] private int _bpm;          // 100-120
    [ObservableProperty] private bool _isPlaying;
    [ObservableProperty] private bool _beatPulse;   // toggles on each beat
    [ObservableProperty] private int _beatCount;

    [RelayCommand] void ToggleMetronome();
    [RelayCommand] void IncreaseBpm();
    [RelayCommand] void DecreaseBpm();
}
```

From AclsTracker/ViewModels/TimerViewModel.cs:
```csharp
public partial class TimerViewModel : ObservableObject
{
    ObservableCollection<TimerModel> Timers { get; }  // bound to UI
    [ObservableProperty] private bool _isSessionActive;

    [RelayCommand] void StartSession();
    [RelayCommand] void StopSession();
    [RelayCommand] void StartTimer(string timerId);
    [RelayCommand] void PauseTimer(string timerId);
    [RelayCommand] void ResetTimer(string timerId);
    [RelayCommand] void NewCprCycle();
    [RelayCommand] void MarkMedicationGiven();
}
```

From AclsTracker/Models/TimerModel.cs:
```csharp
public partial class TimerModel : ObservableObject
{
    string Id, Name;
    TimerType Type;
    [ObservableProperty] TimeSpan _elapsed;
    [ObservableProperty] bool _isRunning;
    TimeSpan? TargetDuration;
    double ProgressPercentage;  // 0.0-1.0
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetronomePulse and TimerCard custom controls</name>
  <files>
    AclsTracker/Controls/MetronomePulse.xaml,
    AclsTracker/Controls/MetronomePulse.xaml.cs,
    AclsTracker/Controls/TimerCard.xaml,
    AclsTracker/Controls/TimerCard.xaml.cs
  </files>
  <read_first>
    - AclsTracker/ViewModels/MetronomeViewModel.cs (BeatPulse property for animation trigger)
    - AclsTracker/Models/TimerModel.cs (Elapsed, ProgressPercentage, IsRunning properties)
    - .planning/phases/01-metronome-timers/01-Metronome-Timers-CONTEXT.md (D-03: pulse animation circle, D-05: digital format 00:00 with progress circle)
    - .planning/research/PITFALLS.md (Pitfall 4: large touch targets, high contrast, min 44x44 points)
  </read_first>
  <action>
1. Create `AclsTracker/Controls/MetronomePulse.xaml` — pulsing circle that animates on each beat (per D-03):
   ```xml
   <?xml version="1.0" encoding="utf-8" ?>
   <ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                x:Class="AclsTracker.Controls.MetronomePulse"
                x:Name="PulseControl">
       <Grid HorizontalOptions="Center"
             VerticalOptions="Center"
             WidthRequest="180"
             HeightRequest="180">
           <!-- Outer ring (static border) -->
           <Ellipse x:Name="OuterRing"
                    WidthRequest="180"
                    HeightRequest="180"
                    Stroke="{StaticResource Primary}"
                    StrokeThickness="3"
                    Fill="Transparent"
                    HorizontalOptions="Center"
                    VerticalOptions="Center" />

           <!-- Inner pulse circle (animates scale on each beat) -->
           <Ellipse x:Name="PulseCircle"
                    WidthRequest="140"
                    HeightRequest="140"
                    Fill="{StaticResource Primary}"
                    Opacity="0.7"
                    HorizontalOptions="Center"
                    VerticalOptions="Center" />

           <!-- BPM display centered on circle -->
           <VerticalStackLayout HorizontalOptions="Center"
                                VerticalOptions="Center"
                                Spacing="0">
               <Label x:Name="BpmLabel"
                      Text="{Binding Bpm}"
                      FontSize="42"
                      FontAttributes="Bold"
                      HorizontalTextAlignment="Center"
                      TextColor="White" />
               <Label Text="BPM"
                      FontSize="14"
                      HorizontalTextAlignment="Center"
                      TextColor="White"
                      Opacity="0.8" />
           </VerticalStackLayout>
       </Grid>
   </ContentView>
   ```

2. Create `AclsTracker/Controls/MetronomePulse.xaml.cs` — handles animation triggered by BeatPulse property changes:
   ```csharp
   namespace AclsTracker.Controls;

   /// <summary>
   /// Visual metronome pulse circle (per D-03).
   /// Animates scale 1.0 → 1.15 → 1.0 on each beat for visual feedback.
   /// Uses CommunityToolkit.Maui animations targeting 60fps (AUDI-02).
   /// </summary>
   public partial class MetronomePulse : ContentView
   {
       public static readonly BindableProperty IsPulsingProperty =
           BindableProperty.Create(
               nameof(IsPulsing),
               typeof(bool),
               typeof(MetronomePulse),
               false,
               propertyChanged: OnIsPulsingChanged);

       public bool IsPulsing
       {
           get => (bool)GetValue(IsPulsingProperty);
           set => SetValue(IsPulsingProperty, value);
       }

       public MetronomePulse()
       {
           InitializeComponent();
       }

       private static async void OnIsPulsingChanged(BindableObject bindable, object oldValue, object newValue)
       {
           if (bindable is MetronomePulse control)
           {
               await control.AnimatePulse();
           }
       }

       /// <summary>
       /// Quick scale pulse: expand to 1.15x then back to 1.0x.
       /// Total duration ~200ms for snappy visual feedback at up to 120 BPM (500ms interval).
       /// Uses Easing.CubicOut for natural deceleration feel.
       /// </summary>
       private async Task AnimatePulse()
       {
           // Cancel any running animation
           PulseCircle.CancelAnimations();

           // Expand
           await PulseCircle.ScaleTo(1.15, 80, Easing.CubicOut);
           // Contract back
           await PulseCircle.ScaleTo(1.0, 120, Easing.CubicIn);
       }
   }
   ```

3. Create `AclsTracker/Controls/TimerCard.xaml` — timer display card with digital format and progress (per D-05):
   ```xml
   <?xml version="1.0" encoding="utf-8" ?>
   <ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                x:Class="AclsTracker.Controls.TimerCard"
                x:Name="TimerCardControl">
       <Frame Padding="12"
              CornerRadius="12"
              BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#1E1E1E}"
              BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"
              HasShadow="False"
              Margin="4">
           <Grid ColumnDefinitions="*, Auto"
                 RowDefinitions="Auto, Auto, Auto"
                 ColumnSpacing="8"
                 RowSpacing="4">
               <!-- Timer name -->
               <Label Grid.Row="0" Grid.Column="0"
                      Text="{Binding Name}"
                      FontSize="14"
                      FontAttributes="Bold"
                      TextColor="{AppThemeBinding Light=#333333, Dark=#CCCCCC}" />

               <!-- Running indicator -->
               <Label Grid.Row="0" Grid.Column="1"
                      Text="●"
                      FontSize="12"
                      TextColor="{Binding IsRunning, Converter={StaticResource BoolToColorConverter}}"
                      HorizontalOptions="End"
                      VerticalOptions="Center"
                      IsVisible="{Binding IsRunning}" />

               <!-- Digital elapsed display (D-05: format 00:00) -->
               <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                      Text="{Binding Elapsed, StringFormat='{0:mm\\:ss}'}"
                      FontSize="32"
                      FontAttributes="Bold"
                      FontFamily="OpenSansRegular"
                      HorizontalTextAlignment="Start"
                      TextColor="{AppThemeBinding Light=#111111, Dark=#FFFFFF}" />

               <!-- Progress bar (D-05: circle substitute — linear progress for simplicity, 
                    can be upgraded to circular in future) -->
               <ProgressBar Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                            Progress="{Binding ProgressPercentage}"
                            ProgressColor="{StaticResource Primary}"
                            HeightRequest="6"
                            IsVisible="{Binding TargetDuration, Converter={StaticResource IsNotNullConverter}}" />
           </Grid>
       </Frame>
   </ContentView>
   ```

4. Create `AclsTracker/Controls/TimerCard.xaml.cs`:
   ```csharp
   namespace AclsTracker.Controls;

   public partial class TimerCard : ContentView
   {
       public TimerCard()
       {
           InitializeComponent();
       }
   }
   ```

5. Verify build: `dotnet build AclsTracker/AclsTracker.csproj`
  </action>
  <acceptance_criteria>
    - AclsTracker/Controls/MetronomePulse.xaml contains `Ellipse` elements (OuterRing + PulseCircle)
    - AclsTracker/Controls/MetronomePulse.xaml contains `Text="{Binding Bpm}"` label
    - AclsTracker/Controls/MetronomePulse.xaml.cs contains `BindableProperty IsPulsingProperty`
    - AclsTracker/Controls/MetronomePulse.xaml.cs contains `PulseCircle.ScaleTo(1.15` (expand animation)
    - AclsTracker/Controls/MetronomePulse.xaml.cs contains `PulseCircle.ScaleTo(1.0` (contract animation)
    - AclsTracker/Controls/TimerCard.xaml contains `Text="{Binding Elapsed, StringFormat=`
    - AclsTracker/Controls/TimerCard.xaml contains `Progress="{Binding ProgressPercentage}"`
    - AclsTracker/Controls/TimerCard.xaml contains `Text="{Binding Name}"`
    - AclsTracker/Controls/TimerCard.xaml.cs exists and compiles
    - `dotnet build AclsTracker/AclsTracker.csproj` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj</automated>
  </verify>
  <done>MetronomePulse control with scale animation triggered by IsPulsing bindable property. TimerCard control with digital elapsed display (mm:ss) and progress bar bound to TimerModel properties.</done>
</task>

<task type="auto">
  <name>Task 2: Build MainPage layout, MainViewModel, wire DI, and configure navigation</name>
  <files>
    AclsTracker/ViewModels/MainViewModel.cs,
    AclsTracker/Views/MainPage.xaml,
    AclsTracker/Views/MainPage.xaml.cs,
    AclsTracker/Converters/BoolToColorConverter.cs,
    AclsTracker/Converters/IsNotNullConverter.cs,
    AclsTracker/MauiProgram.cs,
    AclsTracker/AppShell.xaml
  </files>
  <read_first>
    - AclsTracker/MauiProgram.cs (current DI registrations from Plans 02 and 03)
    - AclsTracker/ViewModels/MetronomeViewModel.cs (metronome commands and properties)
    - AclsTracker/ViewModels/TimerViewModel.cs (timer commands and properties)
    - AclsTracker/Controls/MetronomePulse.xaml (pulse control to embed)
    - AclsTracker/Controls/TimerCard.xaml (timer card to embed)
    - AclsTracker/AppShell.xaml (current navigation setup)
    - AclsTracker/App.xaml (current app entry to understand resource dictionaries)
    - .planning/phases/01-metronome-timers/01-Metronome-Timers-CONTEXT.md (D-04: all timers on same screen)
    - .planning/research/PITFALLS.md (Pitfall 4: large buttons min 44x44, high contrast, minimal screens)
  </read_first>
  <action>
1. Create `AclsTracker/Converters/BoolToColorConverter.cs`:
   ```csharp
   using System.Globalization;

   namespace AclsTracker.Converters;

   /// <summary>
   /// Converts bool to Color. True = Green (running), False = Gray (stopped).
   /// Used by TimerCard running indicator dot.
   /// </summary>
   public class BoolToColorConverter : IValueConverter
   {
       public object? Convert(object? value, Type targetType, object? parameter, CultureInfo culture)
       {
           if (value is bool b && b)
               return Colors.LimeGreen;
           return Colors.Gray;
       }

       public object? ConvertBack(object? value, Type targetType, object? parameter, CultureInfo culture)
           => throw new NotImplementedException();
   }
   ```

2. Create `AclsTracker/Converters/IsNotNullConverter.cs`:
   ```csharp
   using System.Globalization;

   namespace AclsTracker.Converters;

   /// <summary>
   /// Returns true when value is not null. Used to show/hide progress bar
   /// based on whether TargetDuration is set (D-05).
   /// </summary>
   public class IsNotNullConverter : IValueConverter
   {
       public object? Convert(object? value, Type targetType, object? parameter, CultureInfo culture)
           => value is not null;

       public object? ConvertBack(object? value, Type targetType, object? parameter, CultureInfo culture)
           => throw new NotImplementedException();
   }
   ```

3. Create `AclsTracker/ViewModels/MainViewModel.cs` — composite ViewModel combining metronome and timer:
   ```csharp
   using CommunityToolkit.Mvvm.ComponentModel;
   using AclsTracker.ViewModels;

   namespace AclsTracker.ViewModels;

   /// <summary>
   /// Composite ViewModel for MainPage. Exposes both MetronomeViewModel
   /// and TimerViewModel as properties so the single-screen layout (D-04)
   /// can bind to both.
   /// </summary>
   public class MainViewModel
   {
       public MetronomeViewModel Metronome { get; }
       public TimerViewModel Timer { get; }

       public MainViewModel(MetronomeViewModel metronome, TimerViewModel timer)
       {
           Metronome = metronome;
           Timer = timer;
       }
   }
   ```

4. Create `AclsTracker/Views/MainPage.xaml` — single-screen layout with metronome at top, timers below (per D-04: all on same screen). Emergency-optimized: large buttons, high contrast, minimal clutter (per PITFALLS.md Pitfall 4):
   ```xml
   <?xml version="1.0" encoding="utf-8" ?>
   <ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:controls="clr-namespace:AclsTracker.Controls"
                xmlns:vm="clr-namespace:AclsTracker.ViewModels"
                xmlns:converters="clr-namespace:AclsTracker.Converters"
                x:Class="AclsTracker.Views.MainPage"
                Title="ACLS Tracker"
                BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#121212}">

       <ContentPage.Resources>
           <ResourceDictionary>
               <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
               <converters:IsNotNullConverter x:Key="IsNotNullConverter" />
           </ResourceDictionary>
       </ContentPage.Resources>

       <ScrollView>
           <Grid RowDefinitions="Auto, Auto, Auto, Auto, *"
                 Padding="16"
                 RowSpacing="16">

               <!-- ═══ ROW 0: Session controls ═══ -->
               <HorizontalStackLayout Grid.Row="0"
                                      HorizontalOptions="Center"
                                      Spacing="16">
                   <Button Text="INICIAR CÓDIGO"
                           Command="{Binding Timer.StartSessionCommand}"
                           BackgroundColor="#D32F2F"
                           TextColor="White"
                           FontSize="18"
                           FontAttributes="Bold"
                           HeightRequest="56"
                           MinimumWidthRequest="160"
                           CornerRadius="8"
                           IsVisible="{Binding Timer.IsSessionActive, Converter={StaticResource InvertBoolConverter}}" />
                   <Button Text="FINALIZAR CÓDIGO"
                           Command="{Binding Timer.StopSessionCommand}"
                           BackgroundColor="#757575"
                           TextColor="White"
                           FontSize="18"
                           FontAttributes="Bold"
                           HeightRequest="56"
                           MinimumWidthRequest="160"
                           CornerRadius="8"
                           IsVisible="{Binding Timer.IsSessionActive}" />
               </HorizontalStackLayout>

               <!-- ═══ ROW 1: Metronome section ═══ -->
               <VerticalStackLayout Grid.Row="1"
                                    Spacing="12"
                                    HorizontalOptions="Center">

                   <!-- Pulse visualization (D-03) -->
                   <controls:MetronomePulse IsPulsing="{Binding Metronome.BeatPulse}"
                                            BindingContext="{Binding Metronome}" />

                   <!-- Metronome controls: BPM -, toggle, BPM + -->
                   <HorizontalStackLayout HorizontalOptions="Center"
                                          Spacing="12">
                       <Button Text="−"
                               Command="{Binding Metronome.DecreaseBpmCommand}"
                               FontSize="24"
                               WidthRequest="56"
                               HeightRequest="56"
                               CornerRadius="28"
                               BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />

                       <Button Text="{Binding Metronome.IsPlaying, StringFormat='{0}'}"
                               Command="{Binding Metronome.ToggleMetronomeCommand}"
                               FontSize="18"
                               FontAttributes="Bold"
                               WidthRequest="140"
                               HeightRequest="56"
                               CornerRadius="8"
                               BackgroundColor="{AppThemeBinding Light=#1976D2, Dark=#1565C0}"
                               TextColor="White">
                           <!-- Note: Text binding above is a placeholder.
                                Code-behind will set text to "INICIAR" / "DETENER" based on IsPlaying -->
                       </Button>

                       <Button Text="+"
                               Command="{Binding Metronome.IncreaseBpmCommand}"
                               FontSize="24"
                               WidthRequest="56"
                               HeightRequest="56"
                               CornerRadius="28"
                               BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#333333}"
                               TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />
                   </HorizontalStackLayout>

                   <!-- Beat count label -->
                   <Label Text="{Binding Metronome.BeatCount, StringFormat='Compresiones: {0}'}"
                          FontSize="14"
                          HorizontalTextAlignment="Center"
                          TextColor="{AppThemeBinding Light=#666666, Dark=#999999}" />
               </VerticalStackLayout>

               <!-- ═══ ROW 2: Timer grid — all visible (D-04) ═══ -->
               <Grid Grid.Row="2"
                     ColumnDefinitions="*, *"
                     RowDefinitions="Auto, Auto"
                     ColumnSpacing="8"
                     RowSpacing="8">

                   <!-- Timer cards bound to individual timers from ViewModel -->
                   <!-- Total Elapsed -->
                   <controls:TimerCard Grid.Row="0" Grid.Column="0"
                                       BindingContext="{Binding Timer.Timers[0]}" />

                   <!-- CPR Cycle -->
                   <controls:TimerCard Grid.Row="0" Grid.Column="1"
                                       BindingContext="{Binding Timer.Timers[1]}" />

                   <!-- Compressions -->
                   <controls:TimerCard Grid.Row="1" Grid.Column="0"
                                       BindingContext="{Binding Timer.Timers[2]}" />

                   <!-- Epinephrine -->
                   <controls:TimerCard Grid.Row="1" Grid.Column="1"
                                       BindingContext="{Binding Timer.Timers[3]}" />
               </Grid>

               <!-- ═══ ROW 3: Quick action buttons ═══ -->
               <HorizontalStackLayout Grid.Row="3"
                                      HorizontalOptions="Center"
                                      Spacing="12">
                   <Button Text="NUEVO CICLO"
                           Command="{Binding Timer.NewCprCycleCommand}"
                           BackgroundColor="#FF9800"
                           TextColor="White"
                           FontSize="16"
                           FontAttributes="Bold"
                           HeightRequest="52"
                           MinimumWidthRequest="140"
                           CornerRadius="8" />

                   <Button Text="ADRENALINA"
                           Command="{Binding Timer.MarkMedicationGivenCommand}"
                           BackgroundColor="#7B1FA2"
                           TextColor="White"
                           FontSize="16"
                           FontAttributes="Bold"
                           HeightRequest="52"
                           MinimumWidthRequest="140"
                           CornerRadius="8" />
               </HorizontalStackLayout>
           </Grid>
       </ScrollView>
   </ContentPage>
   ```

   Note on layout design:
   - **Session start/stop** — large red button at top, most critical action
   - **Metronome** — center, prominent with pulse visualization
   - **Timer grid** — 2x2 grid showing all 4 timers simultaneously (D-04)
   - **Quick actions** — New cycle + medication buttons for fast one-tap actions
   - **Button sizes** — all >= 52px height, meets 44x44 minimum (Pitfall 4)
   - **Colors** — high contrast, semantic (red=start, orange=cycle, purple=medication)

5. Create `AclsTracker/Views/MainPage.xaml.cs`:
   ```csharp
   using AclsTracker.ViewModels;

   namespace AclsTracker.Views;

   public partial class MainPage : ContentPage
   {
       public MainPage(MainViewModel viewModel)
       {
           InitializeComponent();
           BindingContext = viewModel;
       }
   }
   ```

6. Update `AclsTracker/MauiProgram.cs` — add MainViewModel and MainPage DI registrations.

   Add these registrations after the existing ones (from Plans 02 and 03):
   ```csharp
   builder.Services.AddTransient<MainViewModel>();
   builder.Services.AddTransient<MainPage>();
   ```

   Also add using statements if not present:
   ```csharp
   using AclsTracker.Views;
   ```

   Note: Read MauiProgram.cs first to see current state from Plans 02 and 03, then add the new registrations without removing existing ones.

7. Update `AclsTracker/AppShell.xaml` — set MainPage as the startup page:

   Replace the default ShellContent Route and ContentTemplate to point to MainPage:
   ```xml
   <ShellContent
       Title="ACLS Tracker"
       ContentTemplate="{DataTemplate views:MainPage}"
       Route="MainPage" />
   ```

   Add the views namespace at the top of AppShell.xaml:
   ```xml
   xmlns:views="clr-namespace:AclsTracker.Views"
   ```

   If the default template has a `local:MainPage` reference, update it to `views:MainPage`.

8. Also need to handle the `InvertBoolConverter` used in MainPage.xaml. Create `AclsTracker/Converters/InvertBoolConverter.cs`:
   ```csharp
   using System.Globalization;

   namespace AclsTracker.Converters;

   public class InvertBoolConverter : IValueConverter
   {
       public object? Convert(object? value, Type targetType, object? parameter, CultureInfo culture)
           => value is bool b ? !b : value;

       public object? ConvertBack(object? value, Type targetType, object? parameter, CultureInfo culture)
           => value is bool b ? !b : value;
   }
   ```

   Add it to MainPage.xaml ResourceDictionary:
   ```xml
   <converters:InvertBoolConverter x:Key="InvertBoolConverter" />
   ```

9. Verify build: `dotnet build AclsTracker/AclsTracker.csproj`
  </action>
  <acceptance_criteria>
    - AclsTracker/ViewModels/MainViewModel.cs contains `class MainViewModel` with MetronomeViewModel and TimerViewModel properties
    - AclsTracker/Views/MainPage.xaml contains `xmlns:controls="clr-namespace:AclsTracker.Controls"`
    - AclsTracker/Views/MainPage.xaml contains `controls:MetronomePulse` element with `IsPulsing="{Binding Metronome.BeatPulse}"`
    - AclsTracker/Views/MainPage.xaml contains `controls:TimerCard` elements (4 instances for 2x2 grid)
    - AclsTracker/Views/MainPage.xaml contains buttons: "INICIAR CÓDIGO", "FINALIZAR CÓDIGO", "NUEVO CICLO", "ADRENALINA"
    - AclsTracker/Views/MainPage.xaml contains `Command="{Binding Metronome.ToggleMetronomeCommand}"`
    - AclsTracker/Views/MainPage.xaml contains `Command="{Binding Timer.NewCprCycleCommand}"`
    - AclsTracker/Views/MainPage.xaml.cs contains `BindingContext = viewModel`
    - AclsTracker/Views/MainPage.xaml.cs constructor takes `MainViewModel viewModel` parameter
    - AclsTracker/Converters/BoolToColorConverter.cs exists with `class BoolToColorConverter : IValueConverter`
    - AclsTracker/Converters/IsNotNullConverter.cs exists with `class IsNotNullConverter : IValueConverter`
    - AclsTracker/Converters/InvertBoolConverter.cs exists with `class InvertBoolConverter : IValueConverter`
    - AclsTracker/MauiProgram.cs contains `builder.Services.AddTransient<MainViewModel>()`
    - AclsTracker/MauiProgram.cs contains `builder.Services.AddTransient<MainPage>()`
    - AclsTracker/AppShell.xaml contains `views:MainPage` or references the MainPage view
    - `dotnet build AclsTracker/AclsTracker.csproj` exits 0
  </acceptance_criteria>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj</automated>
  </verify>
  <done>MainPage with single-screen layout: metronome pulse at top, 2x2 timer grid in middle, quick action buttons at bottom. All bindings connected. DI wired. AppShell routes to MainPage as startup. Project compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 1 timing system</name>
  <files>AclsTracker/Views/MainPage.xaml</files>
  <action>
Deploy to Android Emulator or physical device and verify all Phase 1 features work end-to-end.

What was built:
- Audio metronome with Stopwatch-precision timing at 100-120 BPM (AUDI-01)
- Pulsing circle animation synchronized with each beat (AUDI-02, D-03)
- 4 concurrent timers: Total Elapsed, CPR Cycle (2-min), Compressions, Epinephrine (TIME-01)
- Independent start/pause/reset per timer (TIME-02)
- Real-time elapsed display in digital format with progress bars (TIME-03, D-05)
- All timers visible on single screen (D-04)
- Emergency-optimized UI with large buttons, high contrast, semantic colors

Steps to verify:

1. Build and deploy to Android Emulator:
   `dotnet build AclsTracker/AclsTracker.csproj -t:Run -f net8.0-android`
   Or open in Visual Studio and run on emulator.

2. Verify metronome:
   - App launches to MainPage with pulse circle showing "110 BPM"
   - Tap toggle button → hear click sounds at steady rhythm
   - See circle pulse (expand/contract) synchronized with each click
   - Tap "+" → BPM increases to 115 (displayed in circle)
   - Tap "−" → BPM decreases back to 110
   - Tap toggle again → metronome stops, pulse stops
   - Verify BPM doesn't go below 100 or above 120

3. Verify timers:
   - See 4 timer cards in 2x2 grid: "Tiempo Total", "Ciclo RCP", "Compresiones", "Adrenalina"
   - All show "00:00" initially
   - Tap "INICIAR CÓDIGO" → Total Elapsed and CPR Cycle start counting
   - Verify elapsed time updates in real-time (smooth, no freezing)
   - CPR Cycle shows progress bar filling toward 2 minutes
   - Tap "NUEVO CICLO" → CPR Cycle and Compressions timers reset and restart
   - Tap "ADRENALINA" → Epinephrine timer resets and starts counting
   - Tap "FINALIZAR CÓDIGO" → all timers stop
   - Verify Total Elapsed shows correct total duration

4. Verify emergency UX:
   - Buttons are large enough to tap easily (no tiny targets)
   - Text is readable (high contrast)
   - Colors are semantically meaningful (red=start, orange=cycle, purple=medication)
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj</automated>
  </verify>
  <done>User has visually confirmed: metronome audio plays at correct BPM, pulse animation syncs with beats, all 4 timers display and update in real-time, per-timer controls work independently, and UI is emergency-ready (large buttons, high contrast)</done>
</task>

</tasks>

<verification>
- `dotnet build AclsTracker/AclsTracker.csproj` completes with 0 errors
- All views, controls, converters, and MainViewModel present
- DI registrations complete in MauiProgram.cs (all services, ViewModels, views)
- AppShell routes to MainPage as startup
- Visual verification: metronome pulse + 4-timer grid on single screen
</verification>

<success_criteria>
- MetronomePulse control animates scale 1.0→1.15→1.0 on each beat via IsPulsing binding (AUDI-02, D-03)
- TimerCard displays elapsed in mm:ss format with progress bar (D-05)
- MainPage shows all controls on single screen: metronome top, timers middle, actions bottom (D-04)
- All buttons meet 44x44pt minimum touch target (PITFALLS.md Pitfall 4)
- Session start/stop, metronome toggle, BPM +/-, new cycle, medication given all functional
- Full DI chain: MauiProgram → MainPage → MainViewModel → (MetronomeViewModel + TimerViewModel) → Services
</success_criteria>

<output>
After completion, create `.planning/phases/01-metronome-timers/01-04-SUMMARY.md`
</output>
