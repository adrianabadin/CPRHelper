---
phase: 08-fix-main-ui-layout-issues-overlay-defibrillation-banner-compress-timer-cards-remove-cardiac-rhythm-label
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AclsTracker/Views/MainPage.xaml
  - AclsTracker/Controls/NotificationBanner.xaml
  - AclsTracker/Controls/NotificationBanner.xaml.cs
  - AclsTracker/Controls/TimerCard.xaml
  - AclsTracker/ViewModels/TimerViewModel.cs
  - AclsTracker/Controls/RhythmSelector.xaml
  - AclsTracker/ViewModels/EventRecordingViewModel.cs
autonomous: false
requirements:
  - UI-08-A
  - UI-08-B
  - UI-08-C
  - UI-08-D
  - UI-08-E

must_haves:
  truths:
    - "Defibrillation banner overlays content without consuming vertical layout space"
    - "All 6 timer cards have uniform 3-row height (no EN PAUSA row variation)"
    - "Compressions timer displays as T.Comp"
    - "Active rhythm button shows white border highlight"
    - "RITMO CARDIACO header label is removed from rhythm selector"
  artifacts:
    - path: "AclsTracker/Views/MainPage.xaml"
      provides: "Single-row Grid with banner overlay and ScrollView in same cell"
      contains: "ZIndex=\"10\""
    - path: "AclsTracker/Controls/TimerCard.xaml"
      provides: "3-row Grid layout (Name, Elapsed+PauseBtn, ProgressBar)"
      contains: "RowDefinitions=\"Auto, Auto, Auto\""
    - path: "AclsTracker/Controls/NotificationBanner.xaml.cs"
      provides: "Slide-down + fade-in animation on Show, reverse on HideBanner"
      contains: "TranslateTo"
    - path: "AclsTracker/Controls/RhythmSelector.xaml"
      provides: "Rhythm buttons with DataTrigger for active border highlight, no header label"
      contains: "BorderWidth"
    - path: "AclsTracker/ViewModels/EventRecordingViewModel.cs"
      provides: "Per-rhythm IsRhythmX bool properties for DataTrigger binding"
      contains: "IsRhythmRCE"
    - path: "AclsTracker/ViewModels/TimerViewModel.cs"
      provides: "Compressions timer named T.Comp"
      contains: "T.Comp"
  key_links:
    - from: "AclsTracker/Views/MainPage.xaml"
      to: "AclsTracker/Controls/NotificationBanner.xaml"
      via: "Banner and ScrollView in same Grid row (overlay)"
      pattern: "VerticalOptions=\"Start\""
    - from: "AclsTracker/Controls/RhythmSelector.xaml"
      to: "AclsTracker/ViewModels/EventRecordingViewModel.cs"
      via: "DataTrigger Binding to IsRhythmX properties"
      pattern: "Binding=.*IsRhythm"
---

<objective>
Fix three MainPage UI layout issues to recover vertical space and improve visual clarity: (1) make defibrillation banner overlay content instead of consuming a Grid row, (2) compress TimerCard to uniform 3-row layout removing EN PAUSA text, rename compressions to T.Comp, (3) remove RITMO CARDIACO header and add active rhythm button white border highlight.

Purpose: Recover vertical space so ADRENALINA and AMIODARONA drug buttons are visible without scrolling on standard Android screens.
Output: Modified XAML controls and ViewModels with overlay banner, compressed timer cards, and rhythm button highlighting.
</objective>

<execution_context>
@C:/Users/Adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fix-main-ui-layout-issues-overlay-defibrillation-banner-compress-timer-cards-remove-cardiac-rhythm-label/08-CONTEXT.md
@.planning/phases/08-fix-main-ui-layout-issues-overlay-defibrillation-banner-compress-timer-cards-remove-cardiac-rhythm-label/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From AclsTracker/Views/MainPage.xaml (lines 21-29 — current Grid structure to modify):
```xml
<Grid RowDefinitions="Auto,*">
    <controls:NotificationBanner x:Name="BannerControl" Grid.Row="0" VerticalOptions="End" ZIndex="10" />
    <ScrollView Grid.Row="1">
```

From AclsTracker/Controls/TimerCard.xaml (lines 19-21 — current 4-row Grid to compress):
```xml
<Grid ColumnDefinitions="*, Auto"
      RowDefinitions="Auto, Auto, Auto, Auto"
      ColumnSpacing="8" RowSpacing="4">
```

From AclsTracker/Controls/NotificationBanner.xaml (line 11 — current opacity to change):
```xml
Opacity="0.95"
```

From AclsTracker/Controls/NotificationBanner.xaml.cs (lines 42-56 — Show method, lines 62-66 — HideBanner):
```csharp
public void Show(string message, int autoDismissSeconds = 5)
{
    BannerMessage = message;
    IsBannerVisible = true;
    // ... timer setup ...
}
public void HideBanner()
{
    IsBannerVisible = false;
    _dismissTimer?.Stop();
}
```

From AclsTracker/ViewModels/TimerViewModel.cs (line 47 — compressions timer name):
```csharp
_timerService.AddTimer("compressions", "Compresiones", TimerType.Compressions, null);
```

From AclsTracker/ViewModels/EventRecordingViewModel.cs (lines 16-23 — CurrentRhythm + OnChanged):
```csharp
[ObservableProperty]
private CardiacRhythm _currentRhythm = CardiacRhythm.Ninguno;
public string CurrentRhythmDisplay => GetRhythmDisplayName(CurrentRhythm);
partial void OnCurrentRhythmChanged(CardiacRhythm value)
{
    OnPropertyChanged(nameof(CurrentRhythmDisplay));
}
```

From AclsTracker/Controls/RhythmSelector.xaml (lines 8-15 — header label to remove):
```xml
<Label Text="RITMO CARDIACO" FontSize="12" FontAttributes="Bold" TextColor="#999999"
       HorizontalTextAlignment="Center" CharacterSpacing="1" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Banner overlay and TimerCard compression</name>
  <files>AclsTracker/Views/MainPage.xaml, AclsTracker/Controls/NotificationBanner.xaml, AclsTracker/Controls/NotificationBanner.xaml.cs, AclsTracker/Controls/TimerCard.xaml, AclsTracker/ViewModels/TimerViewModel.cs</files>
  <action>
**MainPage.xaml — Banner overlay (UI-08-A):**
1. Remove `RowDefinitions="Auto,*"` from the root Grid (line 21). The Grid should have no RowDefinitions attribute (defaults to single `*` row).
2. Remove `Grid.Row="0"` from NotificationBanner control.
3. Remove `Grid.Row="1"` from ScrollView.
4. Change NotificationBanner `VerticalOptions="End"` to `VerticalOptions="Start"` — banner should stick to top of content area.
5. Keep `ZIndex="10"` on the banner — it must render above the ScrollView.
6. Move the NotificationBanner element AFTER the ScrollView in document order (later = renders on top in MAUI Grid same-cell stacking).

**NotificationBanner.xaml — Opacity fix (UI-08-A):**
1. Change `Opacity="0.95"` to `Opacity="1.0"` on the BannerGrid element (line 11) per user decision for fully opaque background.

**NotificationBanner.xaml.cs — Slide-down + fade animation (UI-08-A):**
1. In `Show()` method, before setting `IsBannerVisible = true`, set initial state: `BannerGrid.TranslationY = -60; BannerGrid.Opacity = 0;`
2. After `IsBannerVisible = true`, add animation: `await Task.WhenAll(BannerGrid.TranslateTo(0, 0, 200, Easing.SinOut), BannerGrid.FadeTo(1.0, 150));`
3. Change `Show()` signature to `async void` to support await.
4. In `HideBanner()`, add reverse animation before setting `IsBannerVisible = false`: `await Task.WhenAll(BannerGrid.TranslateTo(0, -60, 180, Easing.SinIn), BannerGrid.FadeTo(0, 150));`
5. Change `HideBanner()` to `async void`.
6. Use fixed -60px offset (not height-based) to avoid zero-height issue on first render.

**TimerCard.xaml — 3-row compression (UI-08-B):**
1. Change `RowDefinitions="Auto, Auto, Auto, Auto"` to `RowDefinitions="Auto, Auto, Auto"` (line 20).
2. Remove the "EN PAUSA" Label block entirely (lines 56-64, the `Label Grid.Row="2" Grid.Column="0"` with Text="EN PAUSA").
3. Move the pause/resume Button from `Grid.Row="2"` to `Grid.Row="1"` (keep `Grid.Column="1"`).
4. Remove `Grid.ColumnSpan="2"` from the elapsed time Label in Row 1 (line 40) — it now only occupies Column 0 since the pause button takes Column 1.
5. Change the ProgressBar from `Grid.Row="3"` to `Grid.Row="2"` (line 80).
6. All 6 timer cards now have uniform 3-row height since the variable-height EN PAUSA row is gone.

**TimerViewModel.cs — Rename compressions (UI-08-C):**
1. In `InitializeDefaultTimers()` line 47, change `"Compresiones"` to `"T.Comp"`:
   `_timerService.AddTimer("compressions", "T.Comp", TimerType.Compressions, null);`
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj</automated>
  </verify>
  <done>Banner overlays content in same Grid row (no layout space consumed), TimerCard uses 3-row Grid without EN PAUSA, pause button is in Row 1 next to elapsed time, compressions timer displays "T.Comp", banner has slide-in/out animation with full opacity</done>
</task>

<task type="auto">
  <name>Task 2: Rhythm selector header removal and active button highlight</name>
  <files>AclsTracker/Controls/RhythmSelector.xaml, AclsTracker/ViewModels/EventRecordingViewModel.cs</files>
  <action>
**EventRecordingViewModel.cs — Per-rhythm bool properties (UI-08-D):**
1. Add 5 computed bool properties after the `CurrentRhythmDisplay` property:
   ```
   public bool IsRhythmRCE => CurrentRhythm == CardiacRhythm.RCE;
   public bool IsRhythmAESP => CurrentRhythm == CardiacRhythm.AESP;
   public bool IsRhythmAsistolia => CurrentRhythm == CardiacRhythm.Asistolia;
   public bool IsRhythmTV => CurrentRhythm == CardiacRhythm.TV;
   public bool IsRhythmFV => CurrentRhythm == CardiacRhythm.FV;
   ```
2. In the existing `OnCurrentRhythmChanged` partial method, add `OnPropertyChanged` calls for all 5 new properties:
   ```
   OnPropertyChanged(nameof(IsRhythmRCE));
   OnPropertyChanged(nameof(IsRhythmAESP));
   OnPropertyChanged(nameof(IsRhythmAsistolia));
   OnPropertyChanged(nameof(IsRhythmTV));
   OnPropertyChanged(nameof(IsRhythmFV));
   ```
   This follows the exact same pattern as `IsSuggested` and `IsOverThreshold` used on drug buttons and timer cards.

**RhythmSelector.xaml — Remove header + add highlight (UI-08-D, UI-08-E):**
1. Remove the "RITMO CARDIACO" Label entirely (lines 9-15 of current file — the first Label in the VerticalStackLayout).
2. Keep the "Ritmo actual: {0}" subtitle Label.
3. Add `BorderWidth="0"` and `BorderColor="Transparent"` as baseline attributes on ALL 5 rhythm buttons.
4. Add a DataTrigger to EACH rhythm button for active state highlight. For each button, the trigger binds to the corresponding IsRhythmX property. Example for the RCE button:
   ```xml
   <Button.Triggers>
       <DataTrigger TargetType="Button"
                    Binding="{Binding IsRhythmRCE}"
                    Value="True">
           <Setter Property="BorderColor" Value="White" />
           <Setter Property="BorderWidth" Value="2" />
       </DataTrigger>
   </Button.Triggers>
   ```
5. Repeat for all 5 buttons with their matching property: IsRhythmRCE, IsRhythmAESP, IsRhythmAsistolia, IsRhythmTV, IsRhythmFV.
6. When the DataTrigger condition becomes False (rhythm changes away), MAUI restores to the baseline `BorderWidth="0"` declared on the Button — no explicit False trigger needed.
  </action>
  <verify>
    <automated>dotnet build AclsTracker/AclsTracker.csproj</automated>
  </verify>
  <done>RITMO CARDIACO header is removed, "Ritmo actual: X" subtitle remains, active rhythm button shows white 2px border, non-active buttons have no border, border updates reactively when rhythm changes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all UI changes</name>
  <what-built>Three UI layout fixes: (1) overlay defibrillation banner that slides in/out without taking layout space, (2) compressed 3-row TimerCard with T.Comp name and no EN PAUSA text, (3) rhythm selector without RITMO CARDIACO header and with active button white border highlight</what-built>
  <how-to-verify>
    1. Deploy to Android emulator or device
    2. Start a code (INICIAR CODIGO)
    3. **Banner overlay (UI-08-A):** Trigger a defibrillation notification (select TV or FV rhythm, then NUEVO CICLO to trigger pulse check) — verify banner slides down from top OVER content, does NOT push content down, has fully opaque background, auto-dismisses after 8 seconds
    4. **TimerCard uniform height (UI-08-B):** Verify all 6 timer cards have the same height — no extra row on the compressions card
    5. **T.Comp name (UI-08-C):** Verify the compressions timer (row 1, column 0) shows "T.Comp" as its name
    6. **Pause button position:** Verify the pause/resume button on the compressions card appears to the right of the elapsed time (00:00), not in a separate row below
    7. **Active rhythm highlight (UI-08-D):** Select each rhythm button (RCE, AESP, ASISTOLIA, TV, FV) — verify a white border appears on the selected button and disappears from the previously selected button
    8. **Header removed (UI-08-E):** Verify "RITMO CARDIACO" header text is gone — only "Ritmo actual: X" subtitle remains above the rhythm buttons
    9. **Drug buttons visible:** Verify ADRENALINA and AMIODARONA buttons are now visible without scrolling on the main screen
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build AclsTracker/AclsTracker.csproj` passes without errors after all tasks
- Visual check: banner overlays content, all timer cards uniform height, active rhythm button has white border
- No vertical space wasted by banner row or EN PAUSA row or RITMO CARDIACO header
</verification>

<success_criteria>
1. Banner appears floating over content (no layout shift) with slide-in animation
2. All 6 TimerCard controls have identical 3-row height
3. Compressions timer shows "T.Comp" name
4. Active rhythm button has visible white border; inactive buttons have none
5. "RITMO CARDIACO" header is absent from RhythmSelector
6. ADRENALINA and AMIODARONA buttons visible without scrolling
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-main-ui-layout-issues-overlay-defibrillation-banner-compress-timer-cards-remove-cardiac-rhythm-label/08-01-SUMMARY.md`
</output>
